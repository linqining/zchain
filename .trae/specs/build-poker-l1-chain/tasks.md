# Tasks

## Phase 1: 链骨架与对象模型与账户抽象
- [ ] Task 1: 初始化 poker_l1 crate 与工作空间
  - [ ] SubTask 1.1: 创建 `poker_l1/` 目录与 `Cargo.toml`，加入根 workspace
  - [ ] SubTask 1.2: 定义模块结构（consensus/dag / object_model / vm / crypto_precompiles / account / offline / network / bridge / governance / node）
  - [ ] SubTask 1.3: 添加基础依赖（blstrs、solana_rbpf、libp2p、serde、tokio、sha2、rocksdb、bcs、secp256k1、ed25519-dalek、blake2）
- [ ] Task 2: 实现对象模型核心
  - [ ] SubTask 2.1: 定义 `Object`（id、version、owner、type、data、assigned_validator）与 `ObjectID`；**NEW-L4 修复**：`ObjectID = (creator_address: [u8;20], creation_nonce: u64)` 二元组，全局唯一性由创建账户 nonce 单调递增 + 不同 creator address 保证；`ObjectStore` 创建时校验 `ObjectID` 不存在，冲突返回 `ObjectIDCollision`
  - [ ] SubTask 2.2: 定义 `Ownership` 枚举（AddressOwned / Shared / Immutable / ChannelOwner）
  - [ ] SubTask 2.3: 实现 `ObjectStore`（内存版 + Sparse Merkle Tree backing）
  - [ ] SubTask 2.4: 对象 BCS 序列化与 content-hash 计算
- [ ] Task 3: 实现区块与交易结构
  - [ ] SubTask 3.1: 定义 `Block`（header 含 `height` / `timestamp_ms` / `prev_hash` / `state_root` / `public_tx_root` / `gameturn_tx_root` / `dag_commit_certificate`、public_txs、gameturn_txs）
  - [ ] SubTask 3.2: 定义 `Transaction`（inputs、outputs、contract_call、tagged_pubkey、signature、gas、lane_hint、route_hint、**chain_id**、**nonce**、**`gameturn_nonce: Option<u64>`**（NEW-M9 修复 — GameTurn tx 专用 per-game nonce，与 account nonce 解耦，避免 Public tx nonce 阻塞 GameTurn 出牌）、**`is_fallback: bool`**（SEC-H7 修复 — 默认 false，fallback tx 显式标记 true））；**SEC-L3 修复 — `gameturn_nonce` 存储结构**：`Game.player_nonce: BTreeMap<PlayerAddress, u64>`（per-game per-player 计数器），玩家首次 join 时初始化为 0，每个 GameTurn tx（含 fallback tx）执行成功后 `+= 1`；冷启动（map 中无该 player 记录）按 0 处理；该字段随 Game 对象持久化与裁剪
  - [ ] SubTask 3.3: 定义 `DagVertex`（round、author、tx_list、parent_hashes、author_sig）与 `DagCommitCertificate`（round、vertex_hashes、signature_list、signer_bitmap）
  - [ ] SubTask 3.4: 实现区块哈希与链式链接
- [ ] Task 4: 实现基础链存储
  - [ ] SubTask 4.1: `BlockStore`（按 hash / height 索引）
  - [ ] SubTask 4.2: `ObjectStore` 持久化（rocksdb backend）
  - [ ] SubTask 4.3: `DagVertexStore` 持久化（按 hash / round / author 索引）
  - [ ] SubTask 4.4: 全局状态根计算（所有 live 对象的 Sparse Merkle Root）
- [ ] Task 5: 实现多曲线钱包签名（tagged pubkey）
  - [ ] SubTask 5.1: 定义 `SignatureScheme` 枚举（Secp256k1 / Ed25519）与 tagged pubkey 编码（1B tag + raw bytes）；**SEC-M9 修复 — tag 版本化**：tagged pubkey tag 字段编码 `(scheme_id: 4 bits || version_id: 4 bits)`，0x00=secp256k1 v1、0x01=ed25519 v1，新增 tag 需 90% quorum 治理批准，预留 14 个 scheme×version 组合防前向不兼容
  - [ ] SubTask 5.2: 实现 secp256k1 ECDSA recoverable 验证（signature = r||s||v）；**NEW-L1 修复**：强制 low-s（BIP-62）— 校验 `s <= n/2`（n 为曲线阶数），`s > n/2` 返回 `InvalidSignatureLowS`；应用于所有 secp256k1 签名路径（tx / vertex / receipt / operator_ack / ACK）；**SEC-L2 修复 — low-s 检查时机明确**：low-s 校验必须在签名解析后、pubkey 恢复前执行（即解析 `r || s || v` 后立即校验 `s <= n/2`），失败立即返回 `InvalidSignatureLowS` 不进入 ECDSA 验证流程 — 避免 attacker 用 high-s 签名浪费 validator 完整 ECDSA 验证计算资源（DoS 缓解）；通过后接受 tx
  - [ ] SubTask 5.3: 实现 ed25519 验证；**SEC2-L1 修复 — ed25519 签名规范化**：校验 R 编码 canonical（y < 2^255 - 19）；校验 S 编码 canonical（S < L）；非规范化返回 InvalidSignatureCanonical；应用于所有 ed25519 签名路径
  - [ ] SubTask 5.4: 统一 `verify_signature(tagged_pubkey, sig, msg_hash) -> bool` 按 tag 路由，未知 tag 返回 `UnknownScheme`
- [ ] Task 6: 实现账户抽象与交易安全（S7/M9/M10 修复）
  - [ ] SubTask 6.1: 定义 `Account { address, tagged_pubkey, nonce: u64, balance: u64 }`
  - [ ] SubTask 6.2: 地址派生：`address = blake2b_256(tagged_pubkey)[0..20]`
  - [ ] SubTask 6.3: tx 重放保护：validator 校验 `tx.chain_id == network_chain_id` 且 `tx.nonce == account.nonce`，通过后 `account.nonce += 1`；**NEW-M9 修复**：GameTurn 通道 tx 可使用 `gameturn_nonce`（per-game per-player 计数器）替代 account nonce — validator 校验 `gameturn_nonce == game.player_nonce[player]` 后 `+= 1`；GameTurn tx 不再阻塞 account nonce 链（account nonce 仅由 Public / ForceSync tx 推进）；fallback 接受的 GameTurn tx（NEW-H2）同样使用 `gameturn_nonce`
  - [ ] SubTask 6.4: 账户余额管理：Public 通道 tx 扣 gas 从 balance 扣除；余额不足返回 `InsufficientBalance`
- [ ] Task 38: Phase 1 模块测试（基础类型 / 账户 / 存储）
  - [ ] SubTask 38.1: **基础类型单元测试**：TxRequest / Transaction / Block / DagVertex / DagCommitCertificate 的 BCS + JSON 序列化往返；空 vector / 最大长度 / 超限字段返回 `InputTooLong`
  - [ ] SubTask 38.2: **tagged pubkey 单元测试**：secp256k1 / ed25519 签名验证正向 + 反向（错误签名 / 错误 pubkey / 重放）；curve tag 不匹配返回 `CurveMismatch`；signature 长度边界；**NEW-L1 修复**：secp256k1 high-s 签名返回 `InvalidSignatureLowS`（防 malleability）
  - [ ] SubTask 38.3: **ObjectID / UID / Owner 单元测试**：生成唯一性、所有权转移合法路径、越权操作返回 `NotOwner`、immutable owner 不可转移；**NEW-L4 修复**：`ObjectID = (creator_address, creation_nonce)` 全局唯一性 — 同一 creator nonce 单调递增不复用、不同 creator address 不碰撞、冲突返回 `ObjectIDCollision`
  - [ ] SubTask 38.4: **链存储模块集成测试**：put / get / delete / batch_write 接口；按 block height 查询；range scan 正确性；RocksDB 后端崩溃恢复
  - [ ] SubTask 38.5: **账户抽象集成测试**：nonce 单调递增、重放返回 `NonceTooLow` / `NonceTooHigh`；chain_id 不匹配返回 `WrongChainId`；余额不足返回 `InsufficientBalance`
  - [ ] SubTask 38.6: **覆盖率门禁**：Phase 1 模块行覆盖率 >= 80%，安全路径（tagged pubkey / nonce / chain_id）>= 95%

## Phase 2: Narwhal-Bullshark DAG 共识与游戏分配与双模式排序与时间共识
- [ ] Task 7: 实现 tx 双通道分类与客户端路由
  - [ ] SubTask 7.1: 定义 `TxLane` 枚举（Public / GameTurn / CheckpointAnchor / ForceSync）
  - [ ] SubTask 7.2: tx 路由规则：GameTurn + CheckpointAnchor → assigned_validator；ForceSync + Public → 任意 validator（客户端多副本广播）
  - [ ] SubTask 7.3: 定义 `TurnRule`：给定 Game 对象状态，计算 `current_turn`
  - [ ] SubTask 7.4: assigned_validator 校验轮转约束，非当前轮次玩家写交易返回 `NotYourTurn`，允许 read-only
  - [ ] SubTask 7.5: 非 assigned_validator 收到 GameTurn tx 时转发或返回 `NotAssignedValidator`
- [ ] Task 8: 实现 DAG vertex 产出与 game sub-block 嵌入
  - [ ] SubTask 8.1: validator 收到 tx 后装入本地待出 vertex 缓冲（默认 100ms 内必出 vertex）
  - [ ] SubTask 8.2: vertex 组装：tx_list + 引用 ≥2/3 validator 的上一轮 vertex hash + 自身 secp256k1 签名（**R4-H7 修正 — 签名对象 = `hash(chain_id || epoch || round || author_pubkey || vertex_hash || parent_hashes)`（SEC-C1 修复 — 增加 `epoch` 与 `author_pubkey` 字段：epoch 绑定防 epoch 边界 equivocation 证据歧义；author_pubkey 绑定使 slashing 证据归属不依赖 ECDSA recovery 反推，支持多 key 钱包与 signer_bitmap 绑定）**）
  - [ ] SubTask 8.3: vertex 大小上限 `max_vertex_size`（默认 256KB），超出分多个 vertex
  - [ ] SubTask 8.4: assigned_validator 把自己负责 game 的 GameTurn tx 分组为 game sub-block（每 game 一个），sub-block 内按 `(current_turn, arrival)` 排序
  - [ ] SubTask 8.5: GameTurn 通道游戏操作 tx 免 gas，仅校验轮转约束 + 买入锁仓
  - [ ] SubTask 8.6: **Block 内排序（S9 修复）**：同一 vertex 内 GameTurn tx 先于 ForceSync tx 执行；force_sync 判定使用"该 vertex 内已执行 GameTurn tx 后的虚拟 last_action_height"（R4-M4 修正 — 跨 vertex 排序规则：同一 Bullshark commit（round）内所有 vertex 的 GameTurn tx 先于所有 ForceSync/force_advance tx 执行，即 commit 级别的 S9 规则，防攻击者将 force_advance 提交到与 GameTurn tx 不同 vertex 利用 Bullshark 排序不确定性使 force_advance 先执行）（SEC-H6 修复 — 跨 commit force_advance 抢跑防护：跨 commit（不同 block）的 force_advance 判定需额外校验 — force_advance 所在 commit 的前一个 commit 内是否有该 Game 的 GameTurn tx，若有则 last_action_height 视为已更新，force_advance 判定为 false 被拒绝）
  - [ ] SubTask 8.7: **活跃 Game 上限（S8 修复）**：join 时校验玩家活跃 Game 数 <= `max_active_games_per_player`（默认 10），超出返回 `TooManyActiveGames`
  - [ ] SubTask 8.8: vertex 通过 gossipsub 广播给所有 validator（R4-L3 — gossipsub Sybil 攻击防护：validator-only topic 仅接受已认证 peer 加入 — 每个连接需完成 libp2p identify 握手并出示 validator secp256k1 公钥签名（签名对象 `hash(chain_id || peer_id || timestamp)`），节点校验 pubkey 在当前 ValidatorSet 中方可加入并发布；非 validator 全节点可订阅只读但不能发布；防 Sybil 节点灌入垃圾 vertex）（R5-M1 修正 — epoch 边界 ± epoch_transition_window_blocks 内 gossipsub 认证同时接受旧集与新集 validator pubkey grace period，防节点未同步 epoch 边界 block 时拒绝新 validator）；**SEC2-M2 修复 — gossipsub grace period 共识层约束**：grace period 仅网络层容忍，不改变共识层 vertex 有效性；退出 validator 签名的 vertex 被共识层拒绝（不计入 quorum）；grace period 内 equivocation 仍 slashing；commit certificate 验证须校验所有签名 validator 在该 epoch validator 集中
  - [ ] SubTask 8.9: **GameTurn tx 超时后 fallback 接受（NEW-H2 修复 + R3-H4/R3-H5 修正）**：assigned_validator 在 `game_validator_timeout_blocks` 内未装入 GameTurn tx → 客户端可向任意非 assigned_validator 提交该 tx（附 `assigned_validator_timeout_proof` — 含原始 tx + 提交时间戳 + **多副本广播证据（R3-H4 修正 — 需 ≥3 个副本 validator secp256k1 签名见证，使用与 force_checkpoint 相同的阈值公式 `required_witness_count = max(3, floor(checkpoint_multi_replica_count * 2 / 3))`（3-of-5）（R4-H6 修正 — 原 2-of-N 阈值过低，fallback 允许非 assigned_validator 接受 GameTurn tx 绕过轮转排序独占权，2-of-5 合谋即可伪造 timeout_proof 在 assigned_validator 正常时绕过轮转约束）；防伪造）** + round 范围非包含证明，复用 C6 格式）；非 assigned_validator 验证 timeout_proof 后接受装入自己的 vertex；fallback tx 走 Public 通道正常计费 gas（区别于正常 GameTurn 免 gas）；**R3-H5 修正 — fallback tx 执行排序仍按 GameTurn 通道语义**（current_turn 排序），S9 规则同样适用，防 Public 通道与 GameTurn 通道并行执行导致轮转约束校验顺序不确定；fallback tx 使用 `gameturn_nonce`（NEW-M9）；更新 `last_action_height`；**SEC-H7 修复 — fallback tx 格式标识 + nonce 竞态**：tx 格式增加 `is_fallback: bool`（默认 false），fallback tx 显式标记 `is_fallback = true`，validator 据此路由到 `gameturn_nonce` 验证路径；正常 GameTurn tx 不得设 `is_fallback = true`（validator 拒绝）；fallback tx 装入 vertex 时校验 assigned_validator 在 `game_validator_timeout_blocks` 内确实未装入同 `gameturn_nonce` 的 GameTurn tx（复用 C6 sparse Merkle 非包含证明）；若 fallback tx 与原 GameTurn tx 同 `gameturn_nonce` 都进入 commit，以 Bullshark 排序中先执行的为准，后执行的返回 `DuplicateGameTurnNonce` 并可通过 `refund_tx` 退回 gas
- [ ] Task 9: 实现 Bullshark 共识与 block 投影
  - [ ] SubTask 9.1: 实现 DAG commit certificate 检测：某轮 vertex 获得 ≥2/3 validator 引用
  - [ ] SubTask 9.2: 实现 Bullshark 算法对 DAG vertex 线性排序
  - [ ] SubTask 9.3: 从 DAG commit 投影产出 block 序列（block = commit 内 vertex 的 tx 聚合 + 排序）
  - [ ] SubTask 9.4: block header 含 `dag_commit_certificate`（round、vertex_hashes、signature_list、signer_bitmap）
  - [ ] SubTask 9.5: Block 最终性：commit certificate 含 2/3 secp256k1 多签（signer_bitmap + signature_list） → finalized；**SEC2-C1 修复 — DagCommitCertificate 签名域明确**：签名对象 = `hash(chain_id || epoch || commit_round || prev_commit_hash || vertex_hash_list || round_attendance_bitmap || state_root || public_tx_root || gameturn_tx_root)`，绑定 epoch 防 equivocation 证据歧义，绑定 prev_commit_hash 防 long-range attack；新增 commit certificate equivocation slashing（同 `(epoch, commit_round)` 双签 → 踢出 + 罚没）
- [ ] Task 10: 实现 block 验证器
  - [ ] SubTask 10.1: 验证 tx 签名（tagged pubkey）+ chain_id + nonce；**SEC-L4 修复 — 签名域统一加 chain_id**：所有 force_\* / request_\* / checkin / partial_checkin / challenge_delta / checkpoint_anchor / checkpoint_skip / force_checkpoint / rotate_validator_key / refuse_ack / request_ack tx 的签名消息对象必须显式包含 `chain_id` 字段作为首字段（即 `hash(chain_id || ...tx-specific-fields...)`），与 vertex 签名（C1）、ACK 签名（C3）、operator_ack 签名域保持一致；防跨链重放攻击（testnet/devnet/mainnet 同名 tx 跨链重放）；validator 校验签名前先校验 `chain_id == network_chain_id`，不匹配返回 `WrongChainId`
  - [ ] SubTask 10.2: 验证 Public 通道 tx 排序合法（gas/arrival 单调）
  - [ ] SubTask 10.3: 验证 game sub-block 的 turn ordering 约束 + assigned_validator 签名
  - [ ] SubTask 10.4: 验证 GameTurn 通道游戏操作 tx 未扣 gas
  - [ ] SubTask 10.5: 验证 object 版本与所有权 + 两通道状态根转移
  - [ ] SubTask 10.6: 验证 vertex 内排序规则（GameTurn 优先于 ForceSync）
  - [ ] SubTask 10.7: 验证 dag_commit_certificate 的 2/3 secp256k1 多签（signer_bitmap + signature_list）
- [ ] Task 11: 实现时间共识（S10 修复）
  - [ ] SubTask 11.1: block header 写入 `timestamp_ms` 与 `height = prev.height + 1`
  - [ ] SubTask 11.2: validator 校验：`timestamp_ms >= prev.timestamp_ms`（单调不减）且 `timestamp_ms <= prev.timestamp_ms + max_interval_ms`（最大间隔）；**不使用 validator 本地时钟**（避免共识分叉）（R5-L4 修正 — timestamp_ms 为软引用，所有硬截止判定以 block.height 为权威；block 提议者可在合法范围内选任意值不影响安全判定）（R7-M3 修正 — `max_clock_drift_ms` 仅供链下参与者作软参考时钟漂移容忍度，不用于 validator 共识硬校验）（**SEC-M5 修复 — timestamp_ms 合谋风险警示**：block 提议者（DAG 中获得 commit 的 validator）可在 `[prev.timestamp_ms, prev.timestamp_ms + max_interval_ms]` 合法范围内任意选 `timestamp_ms`，存在被合谋 validator 操纵链下参与者辅助参考时钟的风险（如故意压低 timestamp_ms 使链下操作方误判 checkpoint_anchor "未近期提交"提前触发 force_advance）；**安全约束**：(1) 链下参与者触发 `force_advance` / `force_checkpoint` 等逃生 tx 的硬截止判定一律以 `block.height` 为权威，禁止以 `timestamp_ms` 作为触发条件；(2) `timestamp_ms` 仅可用于"显示用"与"非安全相关的软参考"；(3) 任何以 `timestamp_ms` 为依据的安全决策均视为实现错误）
  - [ ] SubTask 11.3: 定义超时参数以 block height 计量：`turn_timeout_blocks` / `hand_max_duration_blocks` / `dispute_window_blocks` / `da_window_blocks` / `checkpoint_interval_blocks` / `game_validator_timeout_blocks`
  - [ ] SubTask 11.4: Game 对象维护 `last_action_height` / `hand_start_height` 字段
  - [ ] SubTask 11.5: 实现轻客户端 block header 订阅接口（含 secp256k1 多签验证 + 2/3 quorum 校验）（R4-L5 — 轻客户端 validator 集同步机制：从 trusted checkpoint 获取 validator_set_hash → 请求后续 ValidatorSetUpdate tx 链 → 逐个验证签名 + quorum → 推导当前 ValidatorSet → 验证最新 commit certificate；允许轻客户端从任意 trusted checkpoint 同步到最新状态无需下载全链历史）（R5-H5 修正 — ValidatorSetUpdate tx 格式：`(epoch, prev_validator_set_hash, new_validator_set, new_validator_set_hash, signer_bitmap, signature_list)`，签名域 `hash(chain_id || epoch || prev_validator_set_hash || new_validator_set_hash)`，threshold = 2/3 of prev_validator_set；hash chain：每个 tx 的 prev_validator_set_hash 必须等于上一个 tx 的 new_validator_set_hash，防中间 tx 被隐瞒）（R5-M8 修正 — 轻客户端状态验证：state_root 为 Sparse Merkle Tree root（对象 keyed by ObjectID）；全节点提供 get_state_proof(object_id) RPC 返回 (object_data, sparse_merkle_path)；轻客户端用 state_root 校验 path 验证对象真实性，支持链下参与者 trustless 验证 Game 状态）
- [ ] Task 12: 实现游戏分配与 epoch 重分配
  - [ ] SubTask 12.1: Game 创建时计算 `assigned_validator = validator_set[hash(G.id, current_epoch) % |V|]`，写入 Game 对象
  - [ ] SubTask 12.2: 实现客户端本地路由发现：`hash(game_id, epoch) % |validator_set|`，零延迟路由
  - [ ] SubTask 12.3: epoch 边界（每 `epoch_length_blocks` 默认 1000 block）自动重分配所有活跃 Game；**NEW-M10 修复 — OffChain 模式 Game epoch 过渡协议**：(a) 操作方在 epoch 边界前 `epoch_transition_window_blocks`（默认 10）内必须提交一次 `checkpoint_anchor`（带 ack）作为过渡锚点，新 assigned_validator 从此锚点继续；(b) 未提交过渡锚点 → 任意参与者触发 `force_advance` 或 `request_revert`；(c) `last_partial_fold` 状态保留，新 assigned_validator 接受后续 `partial_checkin` / `checkin` 校验 `intermediate_commitment` 连续性（proof 验证为无状态密码学操作，assigned_validator 切换不影响）；(d) 过渡期间 `force_checkpoint` 的 `assigned_validator_failure_proof` 仅可指控一个 assigned_validator（旧或新）（R4-H2 修正 — 由链上 `tx 提交时的 current_epoch` 权威决定，非客户端本地判断 — 客户端通过轻客户端获取权威 current_epoch，避免时钟偏差或 epoch 边界同步延迟导致指控错误的 assigned_validator）；**SEC2-H3 修复 — epoch 边界 force_advance 抢跑防护**：操作方未提交过渡锚点 → forfeit 保证金按比例扣除（最低 50%）；epoch 边界 ±窗口内 force_advance 须证明操作方未提交过渡锚点；新 assigned_validator 在窗口内不得接受 force_advance（除非附证据）
  - [ ] SubTask 12.4: assigned_validator 失败自动接管：`game_validator_timeout_blocks`（默认 2（R4-L8 修正 — 原 3 与 turn_timeout_blocks 同值致竞争条件，降为 2 给 fallback tx 留处理窗口）)内未提交含该 game 的 vertex，其他 validator 可在 vertex 中包含该 game 的 tx（DAG 冗余）
- [ ] Task 13: 实现 validator 集与 slashing
  - [ ] SubTask 13.1: 定义 `ValidatorSet`（secp256k1 pubkey 集 + 质押金额），genesis 初始化；**NEW-L3 修复**：新 validator 需经历 `bonding_period_blocks`（默认 = 1 epoch = `epoch_length_blocks`）锁定期，期间可同步链状态但不参与共识出块（R5-H7 修正 — validator 退出 unbonding 期：`unbonding_period_blocks`（默认 = 2 × epoch_length_blocks）退出锁定期，期间不参与共识但质押仍可被 slashing，防 equivocation 后立即退出逃避 slashing；unbonding 期结束无 slashing 证据 → 质押可提取）（R5-L5 修正 — validator 密钥轮换：rotate_validator_key tx（旧密钥签名 hash(chain_id||old_pubkey||new_pubkey||block_height) + 新密钥确认），有 key_rotation_delay_blocks timelock，期间旧密钥仍可用于 slashing 证据）（**SEC-C2 修复 — validator 集下限提升至 5**：原 R5-L7 修正 `>= 3` 在 3-of-3 合谋即可控制共识，且与 SEC-C2 的 OffChain 模式 |V|>=5 强制约束对齐，治理提案校验 `new_validator_set_size >= 5`，拒绝将 validator 集缩减至 < 5 的提案；**SEC-M2 修复 — 单次缩减比例 <= 20%**：治理提案校验 `removed_count / prev_validator_set_size <= 0.2`，防一次踢出过多诚实 validator 导致共识被瞬时控制）（**SEC-M11 修复 — VRF 随机源字段**：ValidatorSet 增加 `vrf_pubkey` 字段，每 epoch 第一个 block 的 proposer 须提交 VRF proof `(random_output, vrf_proof)`，链上验证后写入 `epoch_randomness` 用于 replica_set 计算；validator 集更新时 `vrf_pubkey` 同步更新）；**SEC2-C2 修复 — VRF input 绑定 epoch**：`VRF input = hash(chain_id || epoch || prev_epoch_randomness)`，绑定 epoch 防跨 epoch 重用，绑定 prev_epoch_randomness 形成 randomness hash chain；链上校验 VRF input 含当前 epoch，不匹配返回 `VrfInputMismatch`；**SEC2-M10 修复 — VRF 私钥销毁与 retired 标记**：validator 退出须提交 vrf_key_destroy_proof，未提交则 unbonding 期延长；链上校验 VRF output == 链上 epoch_randomness（不匹配返回 VrfOutputMismatch）；replica_set 计算用链上 epoch_randomness 非 VRF proof 中 random_output；退出 validator vrf_pubkey 标记 retired；**SEC2-M12 修复 — epoch_randomness fallback 不可预测性**：fallback 触发条件 = proposer 在 epoch_transition_window_blocks 内未提交 VRF proof；fallback 时 epoch_randomness = hash(prev_epoch_randomness || genesis_chain_randomness)；连续 3 epoch fallback → 治理调查；DAG 冗余下接替 validator 须提交 VRF proof，非立即 fallback；**SEC2-H4 修复 — rotate_validator_key timelock 密钥约束**：旧密钥仅可用于 slashing 证据，不可签名新 vertex；timelock 期间 validator 可签名但不计入 2/3 quorum；新增 key_compromise_proof 紧急轮换路径（90% quorum）；**SEC2-H5 修复 — epoch 边界 commit certificate grace period**：commit certificate 验证使用 epoch 权威 validator 集（由 epoch 字段决定）；epoch N commit 须 epoch N validator 集 2/3 签名；不存在跨 epoch commit grace period（共识层硬约束，非网络层容忍）；epoch 边界 block 须同时含旧集与新集 2/3 签名（过渡 commit）
  - [ ] SubTask 13.2: Slashing — vertex equivocation：提交两个冲突 vertex + 两个签名证据 → 踢出 + 罚没保证金；**NEW-M15 修复**：`slash_amount = stake * slash_percentage / 100`，`slash_percentage` 默认 100%（全额罚没，可治理）；**SEC2-H2 修复 — 多重 slashing 处理规则**：扣除基数 = 剩余质押（非原始）；优先级：vertex equivocation > commit cert equivocation > 拒收 checkpoint > 停机 > refuse_ack；每项扣除 = 剩余 * slash_percentage / 100；质押耗尽转欠款记录；受害者补偿按优先级分配
  - [ ] SubTask 13.3: Slashing — 停机：连续 `downtime_threshold_blocks`（默认 100）未提交任何 vertex → 治理踢出；**NEW-L2 修复**：停机 validator 罚没 `downtime_slash_percentage`（默认 10%（R4-L1/R5-H1 修正 — 由 5% 提升至 10%，原 5% 比例可能不足以威慑为协助审查而故意停机的 validator））保证金 + 失去出块资格；**SEC-M1 修复 — 停机自动 slashing 路径**：连续 `downtime_threshold_blocks + 2 * epoch_length_blocks` 未提交任何 vertex → 自动 slashing `downtime_slash_percentage`（无需治理介入），治理仅用于争议申辩（validator 申辩网络故障）
  - [ ] SubTask 13.4: 审查缓解：force_* tx 任何 validator 必须接受并装入 vertex；审查证据可作治理踢出依据
  - [ ] SubTask 13.5: **审查调查流程（NEW-H1 修复）**：`force_checkpoint` 触发 assigned_validator 标记 `under_investigation` + `defense_window_blocks`（默认 50）防御窗口；窗口内可提交"未收到证明"申辩免责；窗口内无申辩或申辩无效 → 治理 slashing；窗口内合理申辩 → 豁免 slashing 仅记录审查嫌疑（R4-H5 修正 — 累积惩罚仅申辩失败递增 + 衰减机制：`under_investigation_count` 仅当申辩无效或无申辩时 +1（原"无论申辩是否成功 +1"可被恶意参与者 griefing 诚实 validator）；衰减机制：每 epoch 衰减 1（最低为 0），防止历史指控永久累积）
- [ ] Task 39: Phase 2 模块测试（DAG 共识 / 游戏分配 / 时间共识 / slashing）
  - [ ] SubTask 39.1: **tx 双通道分类单元测试**：GameTurn（含 checkpoint_anchor）/ Public / force_*（含 force_checkpoint）tx 路由正确；checkpoint_anchor 路由到 assigned_validator；force_checkpoint 路由到任意 validator；错误路由返回 `NotAssignedValidator` / `WrongLane`
  - [ ] SubTask 39.2: **DAG vertex 产出单元测试**：vertex 组装正确（tx_list + parent_hashes + secp256k1 签名）；vertex 大小超 256KB 分多个；100ms 缓冲窗口必出 vertex
  - [ ] SubTask 39.3: **Bullshark 共识模块集成测试**：4 validator 并行出 vertex + DAG commit + 线性排序；某轮 vertex 获得 ≥2/3 引用形成 commit certificate；commit certificate 的 secp256k1 多签（signer_bitmap + signature_list）验证
  - [ ] SubTask 39.4: **block 验证器单元测试**：tx 签名（tagged pubkey）+ chain_id + nonce；GameTurn tx 未扣 gas；object 版本与所有权；vertex 内排序（GameTurn 优先于 ForceSync）
  - [ ] SubTask 39.5: **时间共识单元测试**：height 单调递增；timestamp 单调不减 + max_interval 约束；超时以 height 判定（不依赖本地时钟）
  - [ ] SubTask 39.6: **游戏分配单元测试**：`hash(G.id, current_epoch) % |V|` 计算正确；epoch 边界自动重分配；客户端本地路由发现与链上一致
  - [ ] SubTask 39.7: **validator 失败接管集成测试**：assigned_validator 离线 → `game_validator_timeout_blocks`（默认 2（R4-L8 修正））后其他 validator 可在 vertex 中包含该 game 的 tx（DAG 冗余）
  - [ ] SubTask 39.8: **slashing 单元测试**：vertex equivocation 双签证据 → 踢出 + 罚没；停机 slashing；审查证据 slashing；安全路径覆盖率 >= 95%
  - [ ] SubTask 39.9: **覆盖率门禁**：Phase 2 模块行覆盖率 >= 80%，共识 / slashing / 时间共识安全路径 >= 95%

## Phase 3: rBPF VM 与 Syscalls 与合约升级
- [ ] Task 14: 集成 solana_rbpf runtime（含 gas 计费表 M8）
  - [ ] SubTask 14.1: 创建 `rbpf_loader` 模块，加载 `.so` 字节码
  - [ ] SubTask 14.2: 实现 entrypoint 调用与上下文（ContextObject）
  - [ ] SubTask 14.3: **Gas 计费表（M8 修复 + NEW-M16 修复 + R3-M3 修正）**：算术 = 1 gas，内存 = 3 gas，分支 = 2 gas；syscall：object_read = 10，object_write = 20，**`secp256k1_verify` = 500（R3-M3 修正 — 原未列出，用于 tx/vertex/receipt/operator_ack/ACK 签名验证）**，bls_g1_mul = 500，bls_pairing = 5000，hypernova_verify = 50000，groth16_verify = 20000，ipa_verify = 15000，**`verify_failure_proof` = 80000（NEW-M16 修复 — 验证 assigned_validator_failure_proof 含多签验证 + Merkle 非包含证明 + round 完备性校验；SEC-H9 修复 — gas 重算至 80000：原 5000 gas 估算基于"标准 Merkle tree (~1000 gas)"是错误的 — assigned_validator_failure_proof 采用 256-bit sparse Merkle tree（R5-H4 修正），非包含证明 worst-case 需 256 层路径 × 每层哈希 ~200 gas ≈ 51200 gas；加 3×secp256k1_verify(500) + round 校验(~1500) + multi-replica receipt 验证 3×500 ≈ 55700 gas，预留 30% 安全边际上取整至 80000；原 5000 gas 会导致诚实验证方 OutOfGas 拒绝合法 force_checkpoint）**；block gas limit = 50M，tx gas limit = 10M；`force_checkpoint` tx 提交方需预付含 `verify_failure_proof` gas，evidence 验证失败 gas 不退（反 spam）
- [ ] Task 15: 实现核心 syscalls
  - [ ] SubTask 15.1: `object_read(id) -> bytes`
  - [ ] SubTask 15.2: `object_write(id, data)`
  - [ ] SubTask 15.3: `object_create(type, data) -> id`
  - [ ] SubTask 15.4: `emit_event(payload)`
  - [ ] SubTask 15.5: `log` / `panic`
  - [ ] SubTask 15.6: `verify_signature` syscall（暴露 Task 5 给合约）
  - [ ] SubTask 15.7: `get_block_height` / `get_timestamp` syscall（暴露时间共识给合约）
  - [ ] SubTask 15.8: **`verify_failure_proof` syscall（NEW-M16 修复 + SEC-H9 修复）**：暴露给合约层验证 `assigned_validator_failure_proof`（含 multi-replica receipt signatures 多签验证 + Merkle 非包含证明 + round 范围完备性校验），gas = 80000（SEC-H9 修复 — 重算至 80000：256-bit sparse Merkle 非包含证明需 256 层路径 × ~200 gas ≈ 51200 + 3×secp256k1_verify(500) + round 校验 ~1500 + multi-replica receipt 3×500 ≈ 55700，预留 30% 安全边际至 80000；原 5000 gas 估算错误致诚实 force_checkpoint OutOfGas）；供 `force_checkpoint` tx 链上验证调用
- [ ] Task 16: 编写示例合约
  - [ ] SubTask 16.1: minimal Rust 合约模板编译为 BPF
  - [ ] SubTask 16.2: 创建 Game 对象的合约示例
  - [ ] SubTask 16.3: 修改 Game 状态的合约示例
  - [ ] SubTask 16.4: poker 合约 settle 函数含台费逻辑（比例 / 封顶 / 收款方），底池为 0 时跳过台费（M1 修复）
  - [ ] SubTask 16.5: poker 合约 HandStarted 时按 `execution_mode` 分支：OnChain / OffChain
  - [ ] SubTask 16.6: poker 合约 force_advance fold/check 规则：默认 fold，大盲位无人加注时 check（M6 修复）；**SEC2-L5 修复 — fold/check 规则边界修正**：preflop 阶段无人 raise（current_bet == big_blind_amount 且 raise_count == 0）且超时玩家是大盲位 → check；postflop 阶段无人下注（current_bet == 0 且 bet_count == 0）→ 任何超时玩家 check
- [ ] Task 17: 实现合约升级机制（M4 修复）
  - [ ] SubTask 17.1: 合约部署时创建 `UpgradeCap` 对象并 transfer 给部署者
  - [ ] SubTask 17.2: 升级 tx：提交新字节码 + UpgradeCap → 注册新版本，contract_id 不变，version += 1
  - [ ] SubTask 17.3: 旧版本字节码变为不可调用
  - [ ] SubTask 17.3a: **R4-L4 — UpgradeCap 多签/timelock 建议**：强烈建议采用 governance multisig ≥2/3 quorum 或 timelock（延迟 parameter_delay_blocks 默认 2000 个 block 生效，期间可审查 + 紧急撤销）；**SEC-L7 修复 — timelock 共识层强制**：原"不在共识层强制"为安全漏洞 — 单点密钥丢失或单方恶意升级可瞬间替换全合约逻辑窃取资金；现升级为共识层强制：(1) 升级 tx 进入 `upgrade_delay_blocks`（默认 = `parameter_delay_blocks` = 2000）timelock，期间新版本仅注册不可调用，到期后自动生效；(2) timelock 期间 UpgradeCap 持有者可提交 `cancel_upgrade` tx 撤销；(3) timelock 期间任意参与者可提交 `dispute_upgrade` tx 触发治理投票冻结升级；(4) 治理可将特定合约 `upgrade_delay_blocks` 设为 `u64::MAX` 实质冻结；(5) 紧急升级须 90% validator quorum 通过专项提案绕过 timelock；(6) `UpgradeCap` 持有者主体记录在合约部署 tx 中，链上可查；**SEC2-M11 修复 — 紧急升级范围限制**：紧急升级须含 critical_vulnerability_proof；仅允许修复性升级（不可改变资金所有权）；生效后触发安全审计期 1000 blocks，可 dispute_emergency_upgrade；字节码须开源 + 审计报告；受影响资金锁定 1000 blocks
- [ ] Task 40: Phase 3 模块测试（rBPF VM / syscalls / 合约升级）
  - [ ] SubTask 40.1: **rBPF VM 单元测试**：加载合法 .so 字节码；非法字节码返回 `InvalidBytecode`；entrypoint 调用 + ContextObject 传递；gas 计费表正确（算术 1 / 内存 3 / 分支 2）
  - [ ] SubTask 40.2: **syscall 单元测试**：object_read / object_write / object_create / emit_event / log / panic / verify_signature / get_block_height / get_timestamp 各自正向 + 反向（不存在对象 / 越权写 / 事件过大 / panic 触发）
  - [ ] SubTask 40.3: **gas 计费单元测试**：block gas limit 50M / tx gas limit 10M 超限返回 `OutOfGas`；worst-case syscall 计费（bls_g1_mul = 500 / bls_pairing = 5000 / hypernova_verify = 50000 / groth16_verify = 20000 / ipa_verify = 15000）
  - [ ] SubTask 40.4: **示例合约集成测试**：minimal Rust 合约模板编译为 BPF + 部署 + 调用；创建 Game 对象合约；修改 Game 状态合约；poker 合约 settle 函数台费逻辑（比例 / 封顶 / 收款方 / 底池为 0 跳过）
  - [ ] SubTask 40.5: **合约升级集成测试**：UpgradeCap 持有者部署新版本；contract_id 不变 + version += 1；旧版本不可调用；非 UpgradeCap 持有者升级返回 `NotAuthorized`
  - [ ] SubTask 40.6: **HandStarted 分支单元测试**：OnChain / OffChain 模式正确分支；force_advance fold/check 规则（默认 fold / 大盲位无人加注 check）
  - [ ] SubTask 40.7: **覆盖率门禁**：Phase 3 模块行覆盖率 >= 80%，gas 计费 / 合约升级安全路径 >= 95%

## Phase 4: BLS12-381 原生预编译（含子群检查）
- [ ] Task 18: 封装 poker_protocol BLS12-381 为预编译（含子群检查）
  - [ ] SubTask 18.1: `bls12_381_g1_add` / `g1_mul` / `g1_neg`（含 G1 子群检查）
  - [ ] SubTask 18.2: `bls12_381_g2_add` / `g2_mul` / `g2_neg`（含 G2 子群检查）
  - [ ] SubTask 18.3: `bls12_381_pairing_check(a1, b1, a2, b2)`（4 输入子群检查 + worst-case gas）
  - [ ] SubTask 18.4: `bls12_381_hash_to_g1` / `hash_to_g2`（RFC 9380，按 msg 字节线性计费，max 65536B）；**SEC2-L2 修复 — BLS12-381 DST 明确**：DST = `POKER_L1_BLS12381G2_XMD:SHA-256_SSWU_RO_`（固定值，genesis 硬编码，治理不可更改）；runtime 自动附加固定 DST，不允许合约自定义
  - [ ] SubTask 18.5: `bls12_381_miller_loop` / `final_exp`
  - [ ] SubTask 18.6: 子群检查失败返回 `InvalidSubgroup` 错误
- [ ] Task 19: 预编译作为 VM syscall 注册
  - [ ] SubTask 19.1: 注册到 rBPF syscall table
  - [ ] SubTask 19.2: 序列化格式（compressed bytes 48 / 96）
  - [ ] SubTask 19.3: gas 计费按 worst-case（g1_mul = 500，pairing = 5000，hash = 1000 + 10*B）
- [ ] Task 20: 节点级 native API
  - [ ] SubTask 20.1: RPC `secp256k1_aggregate_verify`（DAG consensus 多签验证）+ `bls_verify`（仅合约层 ZK 证明验证）
  - [ ] SubTask 20.2: 复用 `poker_protocol::crypto::Bls12381Curve` 实现
- [ ] Task 41: Phase 4 模块测试（BLS12-381 预编译 / syscall 注册 / 节点 API）
  - [ ] SubTask 41.1: **BLS12-381 G1/G2 操作单元测试**：g1_add / g1_mul / g1_neg / g2_add / g2_mul / g2_neg 结果与 blstrs 直接计算一致；compressed bytes 48 / 96 序列化往返
  - [ ] SubTask 41.2: **子群检查单元测试**：非子群 G1/G2 元素调用任何操作返回 `InvalidSubgroup`；非子群元素调用 pairing 在 pairing 之前被拒绝（DoS 防护）；安全路径覆盖率 >= 95%
  - [ ] SubTask 41.3: **pairing_check 单元测试**：RFC 9380 / 已知测试向量正确；4 输入子群检查；worst-case gas = 5000
  - [ ] SubTask 41.4: **hash_to_g1 / hash_to_g2 单元测试**：RFC 9380 合规；gas = 1000 + 10 * msg.len()；msg > 65536B 返回 `InputTooLong`
  - [ ] SubTask 41.5: **miller_loop / final_exp 单元测试**：与 blstrs 直接计算一致；worst-case gas 计费
  - [ ] SubTask 41.6: **节点级 native API 集成测试**：`secp256k1_aggregate_verify`（DAG consensus 多签验证）正向 + 反向；`bls_verify`（仅合约层 ZK 证明验证）正向 + 反向
  - [ ] SubTask 41.7: **gas 计费单元测试**：所有 syscall worst-case gas 正确；超 block_gas_limit / tx_gas_limit 返回 `OutOfGas`
  - [ ] SubTask 41.8: **模糊测试**：BLS12-381 syscall 至少 10000 个随机输入（含非子群、超长 msg、非法 compressed bytes）；无 panic / 无未捕获错误
  - [ ] SubTask 41.9: **覆盖率门禁**：Phase 4 模块行覆盖率 >= 80%，子群检查 / pairing 安全路径 >= 95%

## Phase 5: 链下执行 + ZK 证明验证 + 通信协议 + 裁剪
- [ ] Task 21: 定义 OfflineState commitment 格式（仅 OffChain 模式）
  - [ ] SubTask 21.1: `OfflineState { game_id, version, state_root, participants, nonce, execution_mode }`
  - [ ] SubTask 21.2: Checkout tx：仅当 `execution_mode = OffChain` 时开局后触发
  - [ ] SubTask 21.3: Checkin tx：验证证明，应用 delta，解锁
  - [ ] SubTask 21.4: OnChain 模式跳过 checkout
- [ ] Task 22: 定义可插拔 ZK verifier 接口与 syscall 注册表
  - [ ] SubTask 22.1: `ZkVerifier` trait + syscall 注册表（热插拔）
  - [ ] SubTask 22.2: 通用 `zk_verify(scheme_id, proof, public_io) -> bool` syscall
- [ ] Task 23: Hypernova verifier（含 public_io 边界 O15）
  - [ ] SubTask 23.1: `Proof` 结构（folded instance、witness commitment、final sumcheck）
  - [ ] SubTask 23.2: **public_io 边界（O15 修复）**：initial_commitment、final_commitment、state_delta_hash、ack_chain_hash、fold_step_count（上限 1000）
  - [ ] SubTask 23.3: 注册 `hypernova_verify`，Fiat-Shamir challenge
  - [ ] SubTask 23.4: MVP：verifier stub
- [ ] Task 24: Groth16 verifier
  - [ ] SubTask 24.1: `Groth16Vk` / `Groth16Proof` 结构
  - [ ] SubTask 24.2: 注册 `groth16_verify`，复用 BLS12-381 pairing（含子群检查）
  - [ ] SubTask 24.3: MVP：verifier stub
  - [ ] SubTask 24.3a: **R4-L2 — Groth16 trusted setup 流程**：链下 MPC ceremony 生成 CRS，verifying_key 通过治理提案上链注册到 ZkVerifierRegistry，proving_key 链下分发，toxic waste 由参与方销毁；该流程不在共识层强制，但 spec 文档化提醒采用 Groth16 电路的合约方必须自行组织可信 setup；**SEC-M10 修复 — Groth16 CRS fingerprint 链上验证**：`ZkVerifierRegistry` 注册 verifying_key 时须同时存储 `crs_fingerprint = blake2b_256(vk.alpha_g1 || vk.beta_g2 || vk.gamma_g2 || vk.delta_g2 || vk.ic)`；`groth16_verify(vk_id, proof, public_inputs)` 时 verifier 先校验 `blake2b_256(stored_vk) == crs_fingerprint`，不匹配返回 `CrsFingerprintMismatch`（防 vk 被恶意替换为攻击者控制的 weak vk 实施 key substitution attack）；`crs_fingerprint` 注册后不可更改，更新 vk 须经治理 90% quorum 通过注册新 `vk_id`（保留旧 vk_id 兼容历史 proof）
- [ ] Task 25: IPA verifier
  - [ ] SubTask 25.1: `IpaProof` 结构
  - [ ] SubTask 25.2: 注册 `ipa_verify`
  - [ ] SubTask 25.3: MVP：verifier stub
- [ ] Task 26: 链下 prover 框架
  - [ ] SubTask 26.1: CCS 电路 trait 定义
  - [ ] SubTask 26.2: Hypernova fold step 实现
  - [ ] SubTask 26.3: 多步折叠循环
  - [ ] SubTask 26.4: 集成 `poker_protocol::zk_shuffle` 作为 CCS 电路
- [ ] Task 27: 实现链下执行通信协议（S2/S3/S4 修复 + 审查截断防护）
  - [ ] SubTask 27.1: 定义 `checkpoint_anchor` tx 类型（**走 CheckpointAnchor 通道，路由到 assigned_validator，与 GameTurn 同路由但独立 lane 不参与 turn ordering，免 gas，通过 gossipsub 广播提交（与 DAG vertex 传播同一 topic，确保所有 validator 包括 assigned_validator 必然收到 — 防栽赃），客户端多副本广播默认 `checkpoint_multi_replica_count`=5 个（NEW-M3 修复：由 3 提升）作为审查检测证据，副本 validator 仅见证不装入 vertex**）：`(game_id, current_turn, state_hash, ack_signatures, opt_out_ack_proof?)`（R5-L2 修正 — checkpoint_anchor 去重：相同 (game_id, checkpoint_seq) 仅首次包含入 vertex 时生效，后续返回 DuplicateCheckpoint）
  - [ ] SubTask 27.2: 操作方每 `checkpoint_interval_blocks`（默认 5）提交 checkpoint_anchor，更新 `last_action_height`；操作方提交后无需观察 on-chain confirmation（被动检测模式，ack_chain 与 on-chain confirmation 解耦）；assigned_validator 拒收由 `force_checkpoint` 逃生 tx 触发
  - [ ] SubTask 27.3: 多方签名 ACK：checkpoint_anchor 必须包含所有活跃参与者的 tagged pubkey 签名 ack，缺少返回 `MissingAck`；opt_out_ack_proof 字段用于 ack_deadline 逾期默认 ACK 场景；**NEW-H3 修复 + R3-H3 修正**：ACK 签名对象为 `hash(chain_id || epoch || game_id || current_turn || state_hash || checkpoint_seq || ack_domain_tag)`（**SEC-C3 修复 — 增加 `epoch` 字段**：防跨 epoch 重放 — 同一 game 在不同 epoch 由不同 assigned_validator 处理；**增加 `ack_domain_tag` 常量字节（0x02）**：与 refuse_ack（0x03）、operator_ack（0x04）等做显式 domain separation；**增加 chain_id 防跨链重放** — testnet/mainnet game_id 碰撞时 ACK 不可重放；domain-separated，绑定 game_id 防跨 Game 重放）；**NEW-M11 修复**：活跃参与者 = 当前手牌未 fold 且未 sit-out 的在座玩家，ACK 集合在 fold 事件时动态收缩（fold tx 上链作为证据）；**SEC2-M9 修复 — ack 签名者身份校验**：validator 校验每个 ack 签名者 tagged pubkey 须在 Game.active_participants 集合中（不匹配返回 AckSignerNotParticipant）；ack_signatures 须覆盖全部 active_participants；同一 participant 多个 ack 仅首个有效；active_participants 在 fold/sit-out 时链上更新
  - [ ] SubTask 27.4: force_advance 判定基于 `block.height > last_action_height + turn_timeout_blocks`（**NEW-C2 修复**：字段统一为 `last_action_height`，不再使用 `last_checkpoint_height` 别名）；force_advance 路由到任意 validator
  - [ ] SubTask 27.5: force_checkin 可行性条件：覆盖两种场景 — (1) 操作方已广播 checkpoint state 但拒绝 checkin（恶意扣留）；(2) 操作方机器故障导致无法提交 checkin（机器故障）；两种场景下其他参与者均可基于已广播 checkpoint state 自行计算 (π', Δ')；纯扣留（无 checkpoint 广播）走 request_revert；**H4 修复 — forfeit 边界判定（NEW-C2 修复：字段统一为 `last_action_height`）**：基于 `last_checkpoint_age = block.height - Game.last_action_height`（纯 timer 驱动，不要求故障证据）；`last_checkpoint_age <= turn_timeout_blocks` → 恶意扣留 → forfeit；`last_checkpoint_age > turn_timeout_blocks` → 机器故障 → 不 forfeit（参与者可重折叠）；判定与 `request_revert` 的 reason 字段语义兼容；**NEW-M4 修复 — designated operator 场景（R3-M1 + R3-M7 修正）**：若操作方为 designated operator（非当前轮次玩家），forfeit 边界加倍为 `last_checkpoint_age <= turn_timeout_blocks * 2`；force_advance 时**无条件豁免当前轮次玩家**（改为 check 而非 fold，**R3-M1 修正 — 不需"证明短暂网络抖动"，与纯 timer 驱动一致；豁免对象是当前轮次玩家而非 designated operator**）；**R3-M7 修正 — check 豁免次数上限**：Game 维护 `designated_operator_check_exemptions` 计数器，达上限（默认 2）后恢复 fold 语义，防恶意 designated operator 循环停发无限拖延；反规避：停发 checkpoint_anchor 超 turn_timeout_blocks 先触发 force_advance（fold 损失筹码）
  - [ ] SubTask 27.5e: **操作方故障恢复流程（3 阶段时间窗口，不要求故障证据）**：实现纯 timer 驱动的恢复流程 — 阶段 1 `turn_timeout_blocks`（操作方可恢复，force_advance 可触发，无 forfeit）；阶段 2 `da_window_blocks` + `recovery_window_blocks`（request_da + 参与者重折叠 force_checkin，窗口内无 forfeit）；阶段 3 forfeit + force_revert（窗口过期 + 无 force_checkin + 操作方未恢复 → forfeit 保证金 + 回退到最后 ACKed checkpoint）；**不要求故障证据**（任何证据可伪造，时间窗口不可伪造）
  - [ ] SubTask 27.5f: **动作日志可选保存协议（H5 修复 — operator ack 签名 + 冲突裁决 + NEW-H5 game_id 绑定 + R3-H3 chain_id 绑定）**：链下执行每一步时，参与者可选保存签名动作日志，**日志格式为 `(game_id, step_index, action, state_hash_before, state_hash_after, participant_tagged_pubkey, participant_signature, operator_tagged_pubkey, operator_ack_signature)`** — 其中 `operator_ack_signature` 为操作方对 `hash(chain_id || game_id || step_index || action || state_hash_before || state_hash_after || participant_tagged_pubkey)` 的签名（**NEW-H5 修复：绑定 game_id 防跨 Game 重放与栽赃 + R3-H3 修正：增加 chain_id 防跨链重放**，确认该动作为规范执行）；保存日志的参与者在操作方故障后可汇集日志 → 校验 `participant_signature` 与 `operator_ack_signature` 有效 → 仅保留双签有效日志 → 从最后 ACKed checkpoint 重新执行 → 重新折叠为 π' → 提交 force_checkin；缺少 `operator_ack_signature` 的日志仅作参考（操作方可事后否认）；未保存日志的参与者放弃重折叠权，只能依赖 request_revert；动作日志非链上数据，不占链空间；操作方亦应保存完整执行轨迹；**H5 冲突裁决**：同一 `(game_id, step_index)` 的冲突日志条目（不同 action 或 state_hash_after）且两者均带有效 `operator_ack_signature` → 操作方 equivocation（双签）→ 链上验证冲突证据（含 game_id 一致性校验，**NEW-H5**）后操作方 forfeit 保证金（与 vertex equivocation slashing 同语义）；仅一方有 operator ack → 以带 ack 条目为准；两方均无 → 无法裁决走 request_revert；**反规避**：操作方无法对 A/B 签不同日志制造混乱（双签直接 forfeit），无法事后否认已签动作（非否认性），无法跨 Game 栽赃（game_id 绑定）；参与者伪造 operator_ack_signature → 签名验证失败 → 伪造方 forfeit
  - [ ] SubTask 27.5a: **审查截断防护 — force_checkpoint 逃生 tx（NEW-H1 + R3-M6 修正）**：定义 `force_checkpoint` tx 类型（任意 validator，escape hatch 类，**走 Public 通道正常计费 gas** — 避免免 gas spam 风险）：`(game_id, current_turn, state_hash, ack_signatures, opt_out_ack_proof?, assigned_validator_failure_proof)`；路由到任意 validator；链上验证 evidence 后接受，更新 `last_action_height = block.height`（**NEW-C2 修复**：统一字段；**R3-H6 修正**：request_ack 不再更新此字段）；**NEW-H1 修复 + R3-M6 修正 — 审查调查 + 累积惩罚**：触发 assigned_validator 审查调查流程（标记 `under_investigation` + `defense_window_blocks` 防御窗口，**SEC-H5 修复 — 申辩机制强化**：申辩须提供 gossipsub 订阅日志 + libp2p 连接日志证明网络可达但未收到该 checkpoint_anchor；≥2/3 validator 佐证其网络可达性 + 日志有效性方可申辩成功（防 gossipsub 网络分区下栽赃 — 原设计"assigned_validator 作为 gossipsub 订阅者必然收到"假设在网络分区下不成立）；申辩有效 → 豁免仅记录嫌疑，无申辩或申辩无效 → 治理 slashing），而非自动 slashing；**累积惩罚**：维护 `under_investigation_count`，达阈值（默认 3）后即使申辩也触发 slashing；标记保留 N epoch 供模式分析；支持 `opt_out_ack_proof` 字段（与 checkpoint_anchor 等价语义）；**SEC2-M3 修复 — force_checkpoint evidence spam DoS 防护**：提交方须预锁 force_checkpoint_deposit（buy_in * 10%），验证失败没收；每 Game 每 turn_timeout_blocks 最多 1 个 force_checkpoint；validator 先 cheap check 再完整验证；全局每 block 最多 5 个 force_checkpoint
  - [ ] SubTask 27.5b: **assigned_validator_failure_proof 验证逻辑**：evidence 包含 `(原始 checkpoint_anchor tx 内容 + gossipsub 广播证据 + multi-replica receipt signatures（≥3 个副本 validator 接收见证签名，**3-of-N 多签阈值，NEW-M3 修复：由 2-of-N 提升至 3-of-N**，N=`checkpoint_multi_replica_count`=5）+ assigned_validator 应出 vertex 但未出的 round 范围 + 非包含证明)`；链上验证：(1) 原始 checkpoint_anchor 内容合法（ACK 完整、state_hash 格式正确）；(2) **≥3 个副本 validator 见证签名有效（multi-replica receipt signatures，防止单一或两个副本合谋伪造）**；(3) **栽赃防护（SEC-H5 修正 — 弱化"必然收到"假设）**：≥3 副本见证证明 tx 已进入 gossip 网络，assigned_validator 作为 gossipsub 订阅者**在 mesh 覆盖范围内应收到**（gossipsub mesh 大小 = `checkpoint_multi_replica_count + 1` 确保 assigned_validator 必在 mesh 中）；**但网络分区下 assigned_validator 可能未收到**，因此 assigned_validator 可在 `defense_window_blocks` 内提交申辩（见 SubTask 27.5a 的 SEC-H5 修复），申辩需提供 gossipsub 订阅日志 + libp2p 连接日志 + ≥2/3 validator 网络可达性佐证；申辩成功则豁免 slashing；(4) assigned_validator 在 `game_validator_timeout_blocks` 内未装入 vertex（通过 DAG round 范围 + 非包含证明，见 SubTask 27.5g）；任一失败 → 拒绝 force_checkpoint + evidence 验证失败 gas 不退
  - [ ] SubTask 27.5c: **委托逃生机制（含 C3 撤销注册表 + NEW-M1/M2 修复）**：定义 `delegated_escape_authorization` 凭证格式（`game_id` + 委托方 `tagged_pubkey` + `expiry_height`（**绝对 block height，NEW-M2 修复**）+ **`credential_nonce`** + 操作方 tagged pubkey 签名；**R4-H7 修正 — 签名对象 = `hash(chain_id || game_id || tagged_pubkey || expiry_height || credential_nonce)`**）；链上 Game 对象维护 `delegated_escape_nonce: u64`（初始 0）；链上验证委托凭证有效性（签名 + **`block.height <= expiry_height`（未过期，NEW-M2）** + game_id 匹配 + **`expiry_height - tx.block_height <= delegated_escape_max_expiry_blocks`（有效期不超限，NEW-M2）** + **`credential_nonce > Game.delegated_escape_nonce`**）+ 代提交方签名后接受 `force_checkpoint`；**NEW-M1 修复 — 凭证一次性消费**：接受后链上执行 `Game.delegated_escape_nonce = credential_nonce`（消费该 nonce），同一凭证不可重复使用；**撤销机制**：定义 `revoke_delegated_escape` tx（任意 validator，正常计费 gas）：操作方签名 → `Game.delegated_escape_nonce += 1` → 所有旧 nonce 凭证失效；新凭证使用 `credential_nonce = Game.delegated_escape_nonce + 1`；默认 `delegated_escape_max_expiry_blocks` 降至 100（缩小滥用窗口）；**SEC2-L4 修复 — credential_nonce 消费时机**：credential_nonce 仅在 force_checkpoint 被接受（evidence 验证通过）时消费；被拒绝时不消费可重新提交；同一 nonce 每 turn_timeout_blocks 最多 1 次；消费时机 = force_checkpoint tx finality 后
  - [ ] SubTask 27.5d: **多副本检测协议（NEW-M3 修复）**：客户端提交 checkpoint_anchor 时多副本广播给 `checkpoint_multi_replica_count`（默认 5，NEW-M3 由 3 提升）个 validator；副本 validator 收到但发现 assigned_validator 在 `game_validator_timeout_blocks` 内未装入 vertex → 签发"审查见证证据"（checkpoint_anchor 内容哈希 + 接收时所在 block height + round 范围 + 副本 validator secp256k1 签名；**R4-H7 修正 — 签名对象 = `hash(chain_id || game_id || content_hash || block_height || round_range)`**）；**需 ≥3 个副本 validator 的见证签名方可构成有效 `assigned_validator_failure_proof`**（NEW-M3：由 2-of-N 提升至 3-of-N，防止单一或两个副本合谋伪造）；该见证证据可附在 `force_checkpoint` 的 `assigned_validator_failure_proof` 中，亦可独立提交治理 slashing 提案；副本 validator 不得直接把 checkpoint_anchor 装入自己的 vertex；副本 validator 签发虚假见证证据 → 治理 slashing（罚没保证金，全额 `slash_percentage = 100%`）；**R4-M8 修正 — 副本 validator 确定性选择：`replica_set = top_N(hash(game_id || epoch || checkpoint_seq || epoch_randomness) % |V|, validator_set, N=checkpoint_multi_replica_count)`，取哈希值排序前 N 个 validator，非客户端自由选择 — 防恶意客户端选择合谋副本 validator 签发虚假见证证据陷害诚实 assigned_validator**（**SEC-M11 修复 — replica_set 引入 VRF 随机源**：原纯确定性哈希可被 attacker 在游戏开始前预测整个游戏周期内所有 checkpoint 的 replica_set 并提前 corrupt 对应 validator；现引入 VRF：每 epoch 第一个 block 的 proposer 提交 VRF proof `(random_output, vrf_proof)`（ECVRF-secp256k1，validator 有专门 `vrf_pubkey` 字段），链上验证后写入 `epoch_randomness`，使 replica_set 不可预测；VRF proof 验证失败的 block 被拒绝；proposer 离线时使用 genesis `chain_randomness` 作为 fallback）（**SEC-C2 修复 — 小 validator 集安全降级强制约束**：原 R5-M2 修正"|V| < 5 时阈值降为 max(2, floor((|V|-1)*2/3))"在 |V|=3 退化为 2-of-2 全合谋、|V|=4 退化为 2-of-3，可伪造 assigned_validator_failure_proof 陷害诚实 validator；现强制：(1) 主网 `chain_id` 下 |V| < 5 时 OffChain 模式 Game 创建被拒绝（返回 `ValidatorSetTooSmallForOffChain`），仅允许 OnChain 模式；(2) |V| >= 5 时方可启用 OffChain 模式；(3) testnet/devnet 不受此约束）（**SEC-H5 修复 — gossipsub 传播保证**：gossipsub 采用 mesh + gossip 混合传播，mesh 大小 = `checkpoint_multi_replica_count + 1`（确保 assigned_validator 必在 mesh 中），消息 TTL >= `game_validator_timeout_blocks` 防消息因 TTL 过期丢失）（R5-L3 修正 — replica_set 计算使用 checkpoint_seq 而非 DAG round，每 checkpoint_anchor 提交时计算一次，稳定 ~5 blocks 无需每 block 重算）
  - [ ] SubTask 27.5g: **round 范围非包含证明（C6 修复）**：定义 `assigned_validator_failure_proof` 中的非包含证明格式 `(epoch, round_range [R, R+k], assigned_validator_pubkey, vertex_list, non_inclusion_proofs)`（**SEC-C1 修复 — 增加 `epoch` 字段**：round 跨 epoch 全局递增，须显式绑定 epoch 以判定 round_range 所属 validator 集；链上校验 `epoch` 与 `round_range` 一致性（round_range 必须完全位于该 epoch 内，跨 epoch 的 round_range 拒绝））；(1) vertex_list 列出 assigned_validator 在 [R, R+k] 内所有 vertex（round + author + vertex_hash + tx_merkle_root）；(2) 完备性证明：通过 DAG commit certificate 结构验证 vertex_list 覆盖所有 round（缺失即不完整）；assigned_validator 某 round 未产出 vertex 时需 ≥2/3 validator 缺席见证签名；(3) non_inclusion_proofs：对每个 vertex 提供 Merkle 非包含证明（checkpoint_anchor tx_hash 不在 tx_merkle_tree 中）；(4) 裁剪约束：证据须在 `vertex_prune_after_blocks`（默认 10000）内提交（R4-M7 修正 — round 缺席见证签名生成机制：每轮 Bullshark commit 形成时，commit certificate 中附带 `round_attendance_bitmap`（第 i 位标记 validator vi 该轮是否产出 vertex）；缺席见证无需独立签名 tx — 直接从 commit certificate 的 bitmap 派生（commit certificate 已含 ≥2/3 validator secp256k1 多签，bitmap 为 signed payload 一部分）；assigned_validator bit=0 即 round 缺席证据，避免要求 validator 主动为"自己没产出 vertex"被动事件单独签名）（R5-H4 修正 — tx_merkle_tree 采用 sparse Merkle tree（以 tx_hash 为 key，256-bit depth）而非标准 Merkle tree；非包含证明格式为 `(tx_hash, sparse_merkle_path)`，叶子值 = empty_placeholder 表示 tx 不存在；worst-case proof size = 8KB，**SEC-H9 修复 — verify_failure_proof gas = 80000（256 层 sparse Merkle 非包含证明需 ~51200 gas + 多签验证 + round 校验 ≈ 55700 gas，预留 30% 至 80000；原 5000 gas 估算错误致诚实 force_checkpoint OutOfGas）**）
  - [ ] SubTask 27.6: **审查截断防护 — ack_deadline 机制（含 NEW-M7 频率限制 + R3-H6 修正）**：定义 `request_ack` tx（任意 validator，免 gas），链上设定 `ack_deadline = block.height + ack_deadline_blocks`（默认 3），写入 Game.`pending_ack_requests`；**R3-H6 修正 — `request_ack` 不再更新 `last_action_height`**（ACK 收集动作非游戏推进，更新会被滥用拖延 force_advance）；**NEW-M7 修复 + R3-H6 修正**：每个 Game 对每个参与者 P 同时只允许 1 个 active `pending_ack_request`（同一 `(game_id, target_participant)` 在 `ack_deadline` 未过期前不得提交新 `request_ack`，违反返回 `PendingAckExists`）；**同一 Game 在 `turn_timeout_blocks` 内最多 `min(活跃参与者数, max_request_ack_per_turn_timeout)` 次 request_ack（默认 = 活跃参与者数，上限 10）无论针对哪个 P**（违反返回 `RequestAckTooFrequent`）（R4-M2 修正 — 原 1 次过度限制正常 ACK 收集，多人游戏需多个参与者 ACK 无法在 1 个 turn_timeout_blocks 内完成），防操作方对不同 P 轮流提交拖延 force_advance（R5-L6 修正 — pending_ack_request 重置：P 提交 ACK 或 refuse_ack 后立即清除 (game_id, P) 的 pending request，无需等 ack_deadline 过期即可重新 request_ack）；**SEC2-H1 修复 — opt_out_ack_proof 滥用防护**：`ack_deadline_blocks` 下限提升至 10（覆盖网络抖动 + DDoS）；操作方提交 request_ack 后须等待 `ack_grace_period_blocks`（默认 3）方可提交 opt_out_ack_proof；批量 request_ack 检测触发审查警报
  - [ ] SubTask 27.7: 定义 `refuse_ack` tx（任意 validator，免 gas）：参与者须在 deadline 内提交 `(game_id, request_id, reason, evidence)`；**R4-H7 修正 — refuse_ack 签名对象 = `hash(chain_id || game_id || request_id || reason)`**；evidence 验证失败 → 该参与者 forfeit 保证金；进入 dispute 流程
  - [ ] SubTask 27.8: **ack_deadline 逾期 opt-out**：`block.height > ack_deadline` 且无 ACK 无 refuse_ack → 视为默认 ACK；操作方提交带 `opt_out_ack_proof` 的 checkpoint_anchor（或 force_checkpoint，若 checkpoint_anchor 被审查），链上验证后接受
  - [ ] SubTask 27.9: 治理 slashing 恶意 refuse_ack：累计 `malicious_refuse_count` >= `malicious_refuse_threshold`（默认 3）→ 罚没保证金分配给被恶意拒绝的操作方
  - [ ] SubTask 27.10: **审查截断容错 — checkpoint_skip 机制**：定义 `checkpoint_skip` tx（任意 validator，免 gas）：`(game_id, skip_segment_start, skip_segment_end, last_known_state_hash, continuity_proof)`；仅更新 `last_action_height` 与 `skip_count += 1`，**不推进 ack_chain_hash**；**SEC-M6 修复 — skip 段 ack 一致性显式校验**：原设计仅校验 skip 段起点 ≥2/3 ACK，但未明确 skip 期间 ACK 集合连续性 — 攻击者可在 skip 段中静默移除参与者使下一 checkpoint ACK 集合人为缩减至 < 2/3 即可伪造；现强制：(1) `start_state_proof` 须包含**完整活跃参与者集合**（与上一正常 checkpoint 的 ack_set 完全一致，不允许在 skip 段中变更 ack_set）；(2) 若 skip 段期间有玩家 fold（链上 fold tx 已确认），ack_set 收缩须在 `start_state_proof` 中显式记录 fold 证据 + 缩减后的 ack_set；validator 校验 ack_set 收缩合法（仅 fold/sit-out 触发）+ 收缩后仍 ≥2/3 活跃玩家；(3) skip 后下一 checkpoint 的 ack_set 须 == skip tx 中记录的 ack_set（含合法收缩），不匹配返回 `AckSetMismatch`；(4) 连续 skip 段间 ack_set 不可静默变化
  - [ ] SubTask 27.11: skip_count 上限 `max_skip_segments`（默认 3），超出则操作方必须提交 `request_revert`
  - [ ] SubTask 27.12: 修改 π 的 public_io 边界：包含 `ack_chain_hash`（仅正常 checkpoint 聚合）+ `skip_count` + `segment_continuity_proof`（段间连续性证明）
  - [ ] SubTask 27.13: 链上 verifier 校验：`skip_count <= max_skip_segments` + segment_continuity_proof 验证；任一失败 → checkin 拒绝，操作方进入 forfeit 流程
  - [ ] SubTask 27.13a: **R4-M6 修正 — continuity_proof 格式与验证**：`continuity_proof = (start_state_proof, end_state_proof)`，`start_state_proof` 为 ≥2/3 参与者 ACK 签名聚合证据（签名对象 `hash(chain_id || game_id || checkpoint_seq || state_hash)`），证明 skip 段起点状态已被确认；`end_state_proof` 待下一 checkpoint 提交时隐式补全；链上校验 start_state_proof 签名有效性 + 签名者 ≥2/3 活跃参与者 + state_hash == last_acked_checkpoint.state_hash；失败 → checkpoint_skip 被拒绝，操作方必须提交 request_revert（R5-H6 修正 — end_state_proof 终态验证：(1) 连续多段 skip 时每段 end_state == 下段 start_state；(2) 游戏以 skip 结束时最后一段 end_state == request_revert 的 last_acked_checkpoint.state_hash；(3) skip 后直接 checkin 时最后一段 end_state == π.initial_commitment；(4) 验证算法 verify_segment_chain() 逐段校验，任一断裂 → checkin 拒绝 + forfeit）
- [ ] Task 28: 强制同步与争议解决（S5/M6 修复）
  - [ ] SubTask 28.1: 链上 Game 对象维护 `last_action_height` / `hand_start_height` / `last_commitment`
  - [ ] SubTask 28.2: `force_advance` tx（路由到任意 validator）：超时推进，默认 fold，大盲位无人加注时 check（M6）；判定基于 block.height（R5-L1 修正 — force_advance 自然频率限制：每次触发后 last_action_height 更新，下一次需再等 turn_timeout_blocks 个 block，即每 turn_timeout_blocks 最多 1 次，无需额外频率限制）
  - [ ] SubTask 28.3: `force_checkin` tx（任意 validator）：基于已广播 checkpoint state 自行计算 (π', Δ')；**覆盖恶意扣留 + 机器故障**两种场景；**H4 修复 — forfeit 边界判定基于 `last_checkpoint_age = block.height - Game.last_action_height`（NEW-C2 修复：字段统一为 `last_action_height`）**：`<= turn_timeout_blocks` → 恶意扣留 → forfeit；`> turn_timeout_blocks` → 机器故障 → 不 forfeit（参与者可汇集签名动作日志从最后 ACKed checkpoint 重新折叠）；**NEW-M4 修复**：designated operator 场景下 forfeit 边界加倍为 `<= turn_timeout_blocks * 2`，且 force_advance 时豁免当前轮次玩家（改为 check 而非 fold）避免 griefing；force_checkin 成功 → 游戏正常结算；判定与 `request_revert` reason 字段语义兼容；反规避：停发 checkpoint_anchor 超 turn_timeout_blocks 先触发 force_advance（fold 损失筹码）（R5-M4 修正 — designated_operator_check_exemptions 计数器在 designated operator 成功提交 checkpoint_anchor 时重置为 0，防 2 次合理网络抖动后永久丧失豁免权）（**SEC-H2 修复 — check_exemptions 重置条件加 state_hash 变化校验**：`designated_operator_check_exemptions` 仅当 designated operator 提交的 checkpoint_anchor 的 `state_hash` 与上一次不同（即有实际进度）时重置为 0；防 designated operator 提交无进度 checkpoint_anchor（state_hash 不变，仅 checkpoint_seq +1）循环重置豁免权无限拖延；原 R5-M4 修正"成功提交 checkpoint_anchor 时重置"被恶意 designated operator 滥用；**SEC-H2 修复 — 无进度检测**：连续 2 次 checkpoint_anchor 的 state_hash 相同 → 视为无进度，exemptions 不重置 + 记录 `no_progress_count`，达阈值（默认 2）触发 `force_revert`）
  - [ ] SubTask 28.4: `request_revert` / `force_revert` tx（任意 validator）：内容为 `(game_id, last_acked_checkpoint, reason)`；`reason` 枚举：`technical_interrupt` / `malicious_withholding` / `data_unavailable`；**`reason=technical_interrupt` → 回退到最后 ACKed checkpoint\_state，操作方不 forfeit**（技术中断豁免）；`reason=malicious_withholding` 或 `data_unavailable` → 回退 + 按 forfeit 规则处置；与故障恢复流程兼容：阶段 1-2 内 technical\_interrupt 无 forfeit；阶段 3 内 technical\_interrupt 仍无 forfeit（reason 优先于阶段判定）；恶意滥用由参与者在阶段 3 提交 `force_revert`（reason=malicious\_withholding）触发 forfeit
  - [ ] SubTask 28.5: **challenge_delta 语义（S5 修复 + NEW-H4 修复）**：从 π 的 public_io 重新派生 state_delta_hash（不需 witness），对比 hash(提交的 Δ) 与 state_delta_hash；不一致 → 提交方 forfeit 保证金，**链上无法从 state_delta_hash 逆推正确 Δ'**（哈希不可逆，NEW-H4 修复），因此触发 `request_revert` 回退到最后 ACKed checkpoint_state 重新结算（而非尝试应用 Δ'）；若 hash(提交的 Δ) == state_delta_hash → Δ 与 π 一致，挑战失败，挑战方 forfeit（恶意挑战惩罚）（R4-L7 — 挑战方保证金机制：challenge_delta 提交方须预锁挑战保证金 = buy_in_amount * challenge_deposit_ratio / 100，**SEC-C4 修复 — `challenge_deposit_ratio` 默认值由 10 提升至 50**（与 forfeit_deposit_ratio 同量级，提高恶意挑战成本防 griefing）可治理 ∈ [1, 100]；挑战成立 → 保证金退还 + 从操作方 forfeit 保证金分得奖励（**SEC-C4 修复 — `challenge_reward_ratio` 默认值由 50 提升至 100**，激励挑战方发现 Δ 不一致因发现需链下重新执行计算成本高）可治理 ∈ [10, 100]；挑战失败 → 保证金没收分配给被挑战方（操作方）作补偿；**SEC-C4 修复 — forfeit 保证金分配规则明确**：挑战成立后操作方 forfeit 保证金分配 = `挑战方得 challenge_reward_ratio %（默认 100%），剩余按 buy_in 比例分配给其他受害者玩家`；防恶意挑战方无成本骚扰）
  - [ ] SubTask 28.6: `request_da` tx（任意 validator）：要求操作方在 `da_window_blocks` 内发布状态；进入操作方故障恢复流程阶段 2；`da_window_blocks` + `recovery_window_blocks` 过期且无 force_checkin 且操作方未恢复 → 触发 `force_revert`
  - [ ] SubTask 28.7: `force_settle` tx（任意 validator）：整局超时兜底结算
  - [ ] SubTask 28.7a: **partial_checkin tx（π_partial — 折叠中断恢复 + NEW-M6/M5 修复）**：定义 `partial_checkin` tx 类型（任意 validator，escape hatch 类，正常计费 gas）：`(game_id, π_partial, folded_step_count=N, intermediate_commitment, ack_chain_partial)`；链上 verifier 验证 π_partial（与最终 π 相同的 public_io 边界格式，`fold_step_count = N`，`final_commitment = intermediate_commitment`）；验证通过后链上记录 `last_partial_fold = (intermediate_commitment, N, π_partial_hash, ack_chain_partial_hash)`，**不应用 Δ（不结算）**，仅作"已折叠到第 N 步"锚点；操作方恢复后从该锚点继续折叠；**NEW-C2 修复**：不推进 `last_action_height`（partial_checkin 非操作方活动，仅锚点）；不触发 forfeit；**SEC-H1 修复 — 提交次数上限**：每 Game 最多提交 `max_partial_checkin_count`（默认 3，可治理 ∈ [1, 10]）次 partial_checkin，超出则操作方必须提交完整 checkin 或触发 `request_revert`（防操作方反复提交 partial_checkin 但 folded_step_count 不递增无限拖延）；**SEC-H1 修复 — 进度校验**：每次 partial_checkin 的 `folded_step_count` 必须严格大于上一次记录的 `N`（防操作方提交无进度锚点拖延），违反返回 `NoProgressPartialCheckin`；可多次提交（每次覆盖 `last_partial_fold`）；**NEW-M6 修复 — ack_chain 前缀校验**：完整 checkin 时链上校验完整 `ack_chain[0..N]` 的哈希 == `last_partial_fold` 中记录的 `ack_chain_partial_hash`，防止操作方切换 ack_chain 上下文；**NEW-M5 修复 — ack_chain_hash 算法**：`ack_chain_hash = MerkleRoot(ack_1 || ack_2 || ... || ack_n)`，每个 `ack_i = hash(chain_id || game_id || current_turn || state_hash || checkpoint_seq || participant_tagged_pubkey || participant_signature)`（与 NEW-H3 签名域一致；R4-H3 修正 — 增加 chain_id 绑定防跨链重放，与 spec.md L637 + checklist.md L104 一致；R4-M5 修正 — Merkle 树采用 RFC 6962 风格 domain separation：叶子节点哈希为 `H(0x00 || ack_i)`，内部节点哈希为 `H(0x01 || left_child || right_child)`，区分叶子与内部节点防二次原像攻击；R4-M1 修正 — 边界情况：空树→`H(0x00 || "")`，单叶子→`H(0x00 || ack_1)`，不平衡树→RFC 6962 filled subtree 补齐 `H(0x00 || empty)`（**SEC-L5 修复 — 明确 empty 叶子值 = 空字节串 `b""`**，非 None/null/默认哈希，防实现歧义导致不同客户端构造不同 Merkle root））；skip 段不参与 ack_chain（R5-M5 修正 — ack_chain_partial_hash = MerkleRoot(ack_chain[0..N])，使用与 ack_chain_hash 完全相同的 RFC 6962 构造，确保 partial_checkin 与完整 checkin 算法一致性）（R5-M6 修正 — checkin tx 签名域：`hash(chain_id || game_id || π_hash || state_delta_hash || new_commitment || ack_chain_hash)`；partial_checkin tx 签名域：`hash(chain_id || game_id || π_partial_hash || folded_step_count || intermediate_commitment || ack_chain_partial_hash)`，均由操作方签名，绑定 chain_id 防跨链重放）；**SEC2-M4 修复 — ack_chain 长度上限**：max_ack_chain_length 默认 1000（可治理 ∈ [100, 10000]），超限须 checkin 或 request_revert；增量 Merkle 树构造 O(log n)；checkpoint_interval_blocks 下限提升至 3
  - [ ] SubTask 28.7b: **partial_checkin 与完整 checkin 衔接**：完整 checkin 时链上校验 π_final 的 `initial_commitment` == 已记录的 `intermediate_commitment` 且 `fold_step_count = M - N`（本次折叠步数）；校验通过 → 应用 Δ 结算 + 清除 `last_partial_fold`；无 partial_checkin 记录时向后兼容（`fold_step_count` 为总步数）；**SEC2-M8 修复 — partial_checkin 与完整 checkin race condition**：完整 checkin 须声明 has_partial_checkin: bool；true 时校验 last_partial_fold 存在且 ack_chain[0..N] 匹配；false 时校验 last_partial_fold 不存在；完整 checkin 后 partial_checkin 被拒（GameAlreadyCheckedIn）；同 commit 时 partial_checkin 先执行
  - [ ] SubTask 28.8: `dispute_window_blocks` 挑战窗口 + fraud proof + 保证金；**SEC2-L6 修复 — 时间窗口边界统一**：所有时间窗口采用 `<=` 边界判定（包含边界）；dispute_window: `block.height <= checkin_block.height + dispute_window_blocks`；turn_timeout_blocks / ack_deadline_blocks / da_window_blocks / recovery_window_blocks 全局统一
  - [ ] SubTask 28.9: forfeit 规则由合约定义（R4-L6 修正 — Game 创建时操作方预锁 forfeit 保证金 = `total_table_buy_in * forfeit_deposit_ratio / 100`（**SEC-C4 修复 — 保证金基数由"操作方 buy_in_amount"改为"桌面总 buy-in（所有玩家 buy-in 之和）"**：原设计在多玩家 game 中操作方 forfeit 100% buy-in 远小于总底池，操作方有强烈动机伪造 Δ 窃取底池；现保证金覆盖桌面总 buy-in 确保 forfeit 足以补偿所有受害者），forfeit_deposit_ratio 默认 100（等额桌面总 buy-in）可治理 ∈ [10, 200]；保证金存入 Game.forfeit_deposit 字段；触发 forfeit 时全额扣除分配给受害参与者（按 buy-in 比例）；forfeit 保证金独立于 slashing 保证金；Game 结算后未触发 forfeit 则退还操作方）（R5-H3 修正 — designated operator forfeit 保证金：若操作方为 designated operator（非玩家，无 buy_in_amount），forfeit 保证金 = `designated_operator_bond_amount`（可治理，**SEC-L8 修复 — 默认 = 桌面所有玩家 buy-in 的中位数 median(buy_in)，避免异常值拉高平均**）；Game 对象增加 `designated_operator_bond` 字段；任命 tx 为 `(game_id, operator_pubkey, bond_amount, expiry_height)` 签名 `hash(chain_id || game_id || operator_pubkey || bond_amount || expiry_height)`）；**SEC2-M7 修复 — designated_operator bond_amount 强制校验**：任命 tx bond_amount 须 == 治理参数 designated_operator_bond_amount（操作方不可自行设置）；bond_amount 须 >= 桌面总 buy-in；任命 tx 须所有参与玩家签名确认
- [ ] Task 29: 实现状态裁剪与存储管理（M2 修复 + 历史数据压缩）
  - [ ] SubTask 29.1: 结算后历史版本裁剪：dispute_window_blocks 过期后可裁剪 Game 对象中间版本，保留最终版本 + state root commitment
  - [ ] SubTask 29.2: **历史 tx 内容压缩为 Merkle commitment**：block 距 finality 过 `tx_prune_after_blocks`（默认 1000）+ block 内所有 Game 结算 + dispute 过期 → 丢弃完整 tx 内容，仅保留 `(tx_hash, tx_type, merkle_proof)`；block header 的 `tx_merkle_root` 永久保留
  - [ ] SubTask 29.3: **DAG vertex 压缩为 vertex commitment（NEW-M13 修复：默认值统一为 10000）**：vertex 所在 round 距 finality 过 `vertex_prune_after_blocks`（默认 10000，NEW-M13 修复：原 tasks.md 此处误写为 1000，与 spec.md L759 的 10000 不一致，现统一为 10000）→ 丢弃 `tx_list` + `parent_hashes` 详情，保留 `(round, author, vertex_hash, tx_count, parent_count, author_sig)`
  - [ ] SubTask 29.4: **ZK proof 归档到 DA 层**：checkin 的 (π, Δ, ack_chain) 所在 Game 结算 + dispute 过期 → π 移到 Walrus DA 层，链上仅保留 `(proof_hash, verification_result, walrus_blob_id)`；archive node 数量 < `archive_node_min_count`（默认 3）时不得裁剪（R5-M7 修正 — Walrus DA 集成规范：(1) 付费由 Game 创建时预扣 da_storage_fee 从 forfeit_deposit/buy_in 扣除；(2) proof_hash = blake2b(π||Δ||ack_chain) 链上存储，检索 blob 后重新计算 hash 验证完整性；(3) da_storage_fee 覆盖 dispute_window_blocks + archive_retention_blocks，blob 过期时 archive node 续费；(4) blob 不可用时 request_historical_data 返回 HistoricalDataUnavailable，不影响链上最终性）（**SEC-M7 修复 — Walrus blob 多副本续费 + 失败处理**：(1) 每个 ZK proof blob 在 Walrus 上传时强制 `replica_count >= 3`（Walrus shard 多副本冗余，单点失效不影响检索）；(2) 续费责任由所有 archive node 共担（任一 archive node 续费成功即可，避免单 archive node 故障导致 blob 过期）；(3) 续费资金来源：Game 创建时预扣的 da_storage_fee 优先，预扣资金耗尽后由 chain treasury 兜底（治理预算），treasury 资金不足时触发"blob 过期警告期"（默认 1000 block）提示社区抢救；(4) 续费失败处理：blob 确认过期（Walrus API 返回 not found）→ 链上状态标记 `proof_blob_expired = true`，`request_historical_data` 返回 `HistoricalDataUnavailable`，但 `proof_hash` 与 `verification_result` 仍永久保留（链上 finality 不受影响，仅历史数据可检索性受损）；(5) 治理可触发"blob 重上传"提案：由任一 archive node 从本地存档重新上传至 Walrus（须附 `proof_hash` 完整性校验））
  - [ ] SubTask 29.5: **永久保留项**：block header / ValidatorSet 变更记录（含 slashing 证据 + 罚没金额）/ 治理参数变更记录（参数名 / 旧值 / 新值 / 生效 height）/ Game 最终结算版本 + 台费分配 / slashing 证据（vertex equivocation / 停机 / 恶意 refuse_ack 累计）（**SEC-M8 修复 — 永久保留项清单补全**：原清单遗漏多项安全审计与争议解决所需证据，补全：(1) `force_checkpoint` evidence 全量（含 `assigned_validator_failure_proof` + multi-replica receipt signatures + 申辩记录 + 申辩佐证 validator gossipsub 日志 hash）；(2) `challenge_delta` 争议证据（含挑战方提交的 state_delta_hash + 旧 commitment + challenge_deposit 扣除记录 + challenge_reward 发放记录）；(3) `request_revert` 回退证据（含回退前 commitment + 回退后 commitment + 触发原因）；(4) ZK proof 的 `proof_hash` + `verification_result` + `walrus_blob_id`（即使 blob 过期也保留 hash 链）；(5) `partial_checkin` 锚点记录（`last_partial_fold` 完整字段，防后续 checkin 衔接断裂）；(6) `rotate_validator_key` tx 完整记录（含旧/新 pubkey + timelock 期 + slashing 证据窗口）；(7) `UpgradeCap` 升级 tx 记录（含新字节码 hash + timelock 期 + cancel/dispute 记录）；(8) `verifier_status` 切换记录（含 chain_id + 旧/新状态 + 治理提案 hash + 投票记录）；(9) validator `under_investigation_count` 累积记录（含每次调查的 evidence hash + 申辩结果 + 衰减历史）；(10) 桥操作 `burn_on_source` + `mint_on_target` 凭证（含 nonce + recipient + source_tx_hash）；所有永久保留项写入 archive node 但 full node 可仅保留最近 N=10000 block 的详情以节省存储）
  - [ ] SubTask 29.6: **节点角色分层**：archive node（永不裁剪）/ full node（Layer 1-3 裁剪）/ light node（仅 block header + state root commitment）
  - [ ] SubTask 29.7: **数据可恢复性**：`request_historical_data(tx_hash | vertex_hash | proof_hash)` RPC；archive node 响应延迟 < 5s；archive node 数量不足时 full node 自动升级为 archive mode（**SEC-M3 修复 — archive node 经济激励**：原"archive node 数量不足时 full node 自动升级为 archive mode"为义务无激励，会导致 full node 消极配合或拒绝升级（存档成本高、无回报）；现改为：(1) 治理预算从 chain treasury 拨付 `archive_reward_per_block`（默认 0，可治理 > 0）按 archive node 数量平分，每个 epoch 结算一次；(2) archive node 须质押 `archive_bond_amount`（默认 = 1 validator 质押金额的 10%），SLA 违约（响应延迟 > 5s 持续 > 1000 block）扣除部分 bond；(3) `request_historical_data` RPC 由请求方支付微 gas（按返回数据量计费），收入归响应的 archive node；(4) 治理可指定 `mandatory_archive_validators`（强制 validator 节点兼 archive 职责，作为兜底）；(5) archive node 数量 < `archive_node_min_count`（默认 3）时，治理触发"招募期"提高 `archive_reward_per_block` 激励自愿升级，而非强制 full node）；**SEC2-M5 修复 — archive node 勾结检测**：archive node 每 epoch 提交存储证明（PoSt），随机抽样历史 block 验证；存储证明失败扣 50% bond，连续 2 次全额扣除 + 移出；参与者可提交拒服务证据治理 slashing；archive node < min_count 时强制 mandatory_archive_validators 兜底
  - [ ] SubTask 29.8: **Compact Block Relay 协同**：short ID 映射表随 tx 内容一起裁剪；archive node 维护完整映射；新节点 fast sync 跳过历史 short ID
- [ ] Task 42: Phase 5 模块测试（链下执行 / ZK verifier / 通信协议 / 强制同步 / 裁剪）
  - [ ] SubTask 42.1: **OfflineState 单元测试**：commitment 格式序列化往返；checkout tx 仅 OffChain 模式触发；checkin tx 验证证明 + 应用 delta；OnChain 模式跳过 checkout
  - [ ] SubTask 42.2: **ZkVerifier 接口单元测试**：trait + syscall 注册表热插拔；`zk_verify(scheme_id, proof, public_io)` 正向 + 反向（未知 scheme_id / 格式错误 proof / public_io 缺失）
  - [ ] SubTask 42.3: **Hypernova verifier 单元测试**：Proof 结构（folded instance、witness commitment、final sumcheck）；public_io 边界（initial_commitment / final_commitment / state_delta_hash / ack_chain_hash / skip_count / segment_continuity_proof / fold_step_count 上限 1000）；MVP stub 接受合法格式
  - [ ] SubTask 42.4: **Groth16 / IPA verifier 单元测试**：Vk / Proof 结构；`groth16_verify` 复用 BLS12-381 pairing；`ipa_verify` 注册；MVP stub
  - [ ] SubTask 42.5: **链下折叠集成测试**：≥2 步 CCS 实例折叠为单个 proof；`poker_protocol::zk_shuffle` 电路作为 CCS 实例可被消费；Fiat-Shamir challenge
  - [ ] SubTask 42.6: **链下通信协议集成测试**：checkpoint_anchor（CheckpointAnchor 通道多副本见证）+ 多方 ACK + force_advance + force_checkin（覆盖恶意扣留 + 机器故障，**H4 修复 — forfeit 边界判定基于 last_checkpoint_age**）+ request_revert + force_checkpoint（含 assigned_validator_failure_proof + 委托逃生 + 多副本检测，**走 Public 通道正常计费 gas**）+ 操作方故障 3 阶段恢复流程 + 动作日志可选保存（**H5 修复 — 含 operator_ack_signature + 冲突裁决 equivocation forfeit**）；操作方被动检测模式（提交后不观察 on-chain confirmation）
  - [ ] SubTask 42.7: **审查截断防护集成测试**：(a) assigned_validator 拒收 checkpoint_anchor → 副本 validator 见证（≥2 个，2-of-N 多签）→ force_checkpoint 提交（附 assigned_validator_failure_proof，走 Public 通道正常计费 gas）→ 链上验证接受 → last_action_height 更新 → assigned_validator 治理 slashing；(b) 委托逃生 — 操作方离线 → watchtower/参与者凭 delegated_escape_authorization 代提交 force_checkpoint → 链上验证委托凭证 + 代提交方签名接受；(c) 多副本检测 — 副本 validator 签发审查见证证据 → 独立提交治理 slashing 提案；(d) request_ack → ack_deadline 逾期 → opt_out_ack_proof 验证（通过 checkpoint_anchor 或 force_checkpoint）；(e) refuse_ack + 无效 evidence → forfeit 保证金；(f) checkpoint_skip → skip_count 累计 → 最终 checkin π 验证 skip_count + segment_continuity_proof；(g) skip_count > max_skip_segments → 强制 request_revert；(h) 伪造 assigned_validator_failure_proof（无副本 validator 见证 / 见证签名不足 2 个 / 见证签名无效 / round 范围不正确）→ force_checkpoint 拒绝；(i) 副本 validator 签发虚假见证证据 → 治理 slashing
  - [ ] SubTask 42.7a: **操作方故障恢复流程集成测试**：(a) 阶段 1 — 操作方在 turn_timeout_blocks 内恢复 → 提交 checkpoint_anchor → 游戏继续，无 forfeit；(b) 阶段 1 — 操作方未恢复 → force_advance 触发推进轮次，无 forfeit；(c) 阶段 2 — 操作方未恢复 + 参与者有动作日志（含 operator_ack_signature）→ 汇集日志 → 校验双签有效 → 从最后 ACKed checkpoint 重新折叠 → force_checkin 成功 → 游戏正常结算，操作方不 forfeit；(d) 阶段 2 — 操作方恢复 → 操作方继续折叠，无 forfeit；(e) 阶段 3 — da_window_blocks + recovery_window_blocks 过期 + 无 force_checkin + 操作方未恢复 → force_revert → 操作方 forfeit 保证金 → 游戏从最后 ACKed checkpoint 继续；(f) 参与者无动作日志 → 无法重折叠 → 只能等阶段 3 force_revert；**(g) H4 — force_checkin forfeit 边界 — last_checkpoint_age <= turn_timeout_blocks → 恶意扣留 → forfeit**；(h) H4 — force_checkin forfeit 边界 — last_checkpoint_age > turn_timeout_blocks → 机器故障 → 不 forfeit；**(i) H5 — 动作日志冲突 — operator equivocation — 操作方对 A/B 签同一 step_index 不同 action → 两方均带有效 operator_ack_signature → 链上验证 → 操作方 forfeit**；(j) H5 — 动作日志冲突 — 单方签名 → 以带 ack 条目为准；(k) H5 — 参与者伪造 operator_ack_signature → 签名验证失败 → 伪造方 forfeit
  - [ ] SubTask 42.8: **强制同步单元测试**：force_advance（默认 fold / 大盲位 check）；force_checkin 基于已广播 checkpoint state（覆盖恶意扣留 + 机器故障，**H4 修复 — forfeit 边界判定基于 last_checkpoint_age**）；request_revert / force_revert 回退（forfeit 仅阶段 3）；challenge_delta 从 π public_io 重派生 Δ'；request_da + recovery_window_blocks + force_revert；force_settle 整局超时；**partial_checkin（π_partial）折叠中断恢复 — 提交 π_partial 锚点 → 恢复后从锚点继续折叠 → 完整 checkin 衔接校验 initial_commitment + fold_step_count**；**H5 动作日志冲突裁决 — operator equivocation 双签检测 + 单方签名优先 + 伪造签名拒绝**
  - [ ] SubTask 42.9: **状态裁剪集成测试**：(a) Game 结算 + dispute 过期 → 中间版本裁剪；(b) block 过 tx_prune_after_blocks → tx 压缩为 (tx_hash, tx_type, merkle_proof)；(c) vertex 过 vertex_prune_after_blocks → 压缩为 vertex commitment；(d) ZK proof 归档到 Walrus DA 层 + walrus_blob_id 可恢复；(e) archive node < archive_node_min_count 时不得裁剪
  - [ ] SubTask 42.10: **节点角色分层集成测试**：archive node 保留全数据；full node Layer 1-3 裁剪；light node 仅 block header；`request_historical_data` RPC 工作；archive node 不足时 full node 自动升级
  - [ ] SubTask 42.11: **模糊测试**：ZK verifier + checkpoint_skip + ack 验证 + force_checkpoint assigned_validator_failure_proof + delegated_escape_authorization + **动作日志 operator_ack_signature 冲突裁决** 至少 10000 个随机输入；伪造证明 / 伪造 ACK / 伪造 continuity_proof / 伪造见证签名（含不足 2-of-N 阈值）/ 伪造委托凭证 / **伪造 operator_ack_signature / 伪造 equivocation 冲突证据** 被拒绝
  - [ ] SubTask 42.12: **覆盖率门禁**：Phase 5 模块行覆盖率 >= 80%，ZK verifier / ACK 验证 / 裁剪 / 强制同步安全路径 >= 95%

## Phase 6: 网络与节点与治理
- [ ] Task 30: P2P 网络（含 M12 网络约束 + 无 mempool）
  - [ ] SubTask 30.1: libp2p gossipsub for DAG vertex / tx 传播
  - [ ] SubTask 30.2: peer discovery
  - [ ] SubTask 30.3: sync protocol（按 range 请求 blocks + DAG vertex + fast sync）
  - [ ] SubTask 30.4: 轻客户端 block header 订阅协议（secp256k1 多签验证 + 2/3 quorum）
  - [ ] SubTask 30.5: **Compact Block Relay（M12 修复）**：先广播 compact vertex（header + tx short IDs），接收节点从本地已收 tx 匹配；**SEC2-L3 修复 — short ID 冲突处理**：short ID = 8 字节（64 bit）；多 tx 匹配同一 short ID 时请求完整 tx hash 消歧；映射表大小有上限；冲突时请求完整 vertex fallback
  - [ ] SubTask 30.6: **Block/tx/vertex 大小上限（M12 修复）**：block <= 4MB，tx <= 128KB，vertex <= `max_vertex_size`（默认 256KB）
  - [ ] SubTask 30.7: **无 mempool（O1 移除）**：validator 收到 tx 后直接装入下一个 vertex，不维护 gossiped pending tx pool；validator 内存中仅保留待装 vertex 的 tx 短暂缓冲（默认 100ms）
  - [ ] SubTask 30.8: 客户端多副本广播：Public tx + force_* tx（含 force_checkpoint）广播给多 validator 副本以提高确定性；checkpoint_anchor 多副本广播作为审查检测证据（副本 validator 仅见证不装入 vertex）
- [ ] Task 31: RPC server
  - [ ] SubTask 31.1: JSON-RPC：`get_block`、`get_object`、`get_tx`、`submit_tx`、`get_account`、`get_dag_vertex`
  - [ ] SubTask 31.2: WebSocket：subscribe events
  - [ ] SubTask 31.3: `secp256k1_aggregate_verify` / `bls_verify`（ZK）/ `zk_verify` RPC
- [ ] Task 32: 节点二进制
  - [ ] SubTask 32.1: validator 节点（含 DAG vertex 产出 + Bullshark 共识 + game sub-block）
  - [ ] SubTask 32.2: full node（validation only，不参与共识，执行 Layer 1-3 裁剪）
  - [ ] SubTask 32.3: archive node（永不裁剪，提供 `request_historical_data` RPC）
  - [ ] SubTask 32.4: light node（仅 block header + state root commitment 订阅）
  - [ ] SubTask 32.5: CLI 工具（keygen 支持 secp256k1/ed25519 tagged pubkey、query、deploy contract、send tx、upgrade contract、本地计算 assigned_validator、请求历史数据）
- [ ] Task 33: 实现治理与参数管理（M11 修复 + NEW-M8/M12/M15/C1 修复）
  - [ ] SubTask 33.1: 参数调整提案（parameter_name, new_value）+ 投票期 `voting_period_blocks`
  - [ ] SubTask 33.2: 2/3 validator 赞成 → 参数生效；**NEW-M8 修复 + R3-M4 修正**：参数**不立即生效**，进入 `parameter_delay_blocks`（**默认 2000（R3-M4 由 500 提升）**）timelock 延迟期；**敏感参数需 90% validator 赞成（R3-H1 修正 — 补全列表）**：`block_gas_limit` / `epoch_length_blocks` / `validator_set` 更新 / **`slash_percentage` / `downtime_slash_percentage` / `verifier_status`（否则 2/3 合谋可降级为 Stub 使操作方伪造 π）/ `parameter_delay_blocks`（否则可降至 0 使 timelock 失效）/ `defense_window_blocks`（否则可降至 0 使被指控 validator 无申辩时间）**；**SEC-H4 修复 — 敏感参数 90% quorum 补全 9 项**：`bonding_period_blocks`（否则 2/3 合谋降至 epoch_length_blocks 缩短 Sybil 攻击准备时间）/ `unbonding_period_blocks`（否则 2/3 合谋降至 epoch_length_blocks 使 validator equivocation 后快速退出提取质押）/ `key_rotation_delay_blocks`（否则 2/3 合谋降至 100 使旧密钥 slashing 证据窗口过短）/ `checkpoint_multi_replica_count`（否则 2/3 合谋降至 3 使合谋门槛退化为 3-of-3）/ `archive_retention_blocks`（否则 2/3 合谋降至 1000 使 Walrus blob 快速过期历史 ZK proof 丢失）/ `max_skip_segments`（否则 2/3 合谋升至 10 削弱 ack_chain 完整性）/ `turn_timeout_blocks`（否则 2/3 合谋降至 3 使链下参与者因区块传播延迟误判超时 force_advance 误触发）/ `malicious_refuse_threshold`（否则 2/3 合谋升至 100 使恶意 refuse_ack 几乎永不触发 slashing）/ `max_request_ack_per_turn_timeout`（否则 2/3 合谋降至 1 阻断多人游戏 ACK 收集）；**SEC-C2 修复 — `validator_set_size` 亦列入 90% quorum 敏感参数**（防 2/3 合谋缩减 validator 集至 < 5）；**SEC-H8 修复 — timelock 撤销机制明确**：timelock 内可由 **≥ 90% validator 赞成**（高于原通过 quorum，防原通过方 2/3 合谋阻止撤销）的反对提案撤销，撤销提案**无 timelock 立即生效**（防原提案在 timelock 内生效）；撤销提案仅可在原提案 timelock 期间提交，timelock 结束后无法撤销（已生效参数需重新发起反向提案）；**SEC2-M6 修复 — 治理投票 quorum 分母明确**：分母 = 当前 epoch 权威 validator 集大小（含离线）；投票参与率下限 = 2/3（敏感参数 90%）；未达参与率下限提案自动否决；投票期 validator 离线率突增 > 30% 可延长投票期
  - [ ] SubTask 33.3: **NEW-M8 修复 — 参数上下界约束**：`turn_timeout_blocks ∈ [3, 1000]`、`max_interval_ms ∈ [500, 60000]`、`block_gas_limit ∈ [10M, 200M]`、`epoch_length_blocks ∈ [100, 10000]`、`slash_percentage ∈ [1, 100]`、`downtime_slash_percentage ∈ [1, 100]`、`parameter_delay_blocks ∈ [100, 10000]`、`defense_window_blocks ∈ [10, 1000]`、`checkpoint_multi_replica_count ∈ [3, 15]`、`delegated_escape_max_expiry_blocks ∈ [10, 1000]`（R4-H4 修正 — 否则 slash_percentage 可降至 0 使 equivocation 无经济惩罚）；超出上下界的提案被拒绝（R5-H2 修正 — `game_validator_timeout_blocks ∈ [1, floor(turn_timeout_blocks / 2)]`，否则治理可设为 > turn_timeout_blocks 使 fallback 机制失效）（R5-M3 修正 — 补全可治理参数上下界：ack_deadline_blocks ∈ [1,100]、max_skip_segments ∈ [1,10]、max_active_games_per_player ∈ [1,1000]、bonding_period_blocks ∈ [epoch_length_blocks, 10*epoch_length_blocks]、downtime_threshold_blocks ∈ [10,10000]、voting_period_blocks ∈ [10,10000]、under_investigation_threshold ∈ [1,100]、max_designated_operator_check_exemptions ∈ [0,10]、hand_max_duration_blocks ∈ [turn_timeout_blocks*4, 100000]、archive_node_min_count ∈ [1,100]、recovery_window_blocks ∈ [10,10000]、checkpoint_interval_blocks ∈ [1,1000]、da_window_blocks ∈ [10,10000]、dispute_window_blocks ∈ [10,10000]、tx_prune_after_blocks ∈ [100,100000]、epoch_transition_window_blocks ∈ [1,100]）（R7-H2/M1/M2/M3/M4/M5 修正 — 补全参数边界：unbonding_period_blocks∈[epoch_length_blocks,10*epoch_length_blocks]、malicious_refuse_threshold∈[1,100]、max_request_ack_per_turn_timeout∈[1,100]、max_vertex_size∈[64KB,4MB]、designated_operator_bond_amount∈[1,10^9]、key_rotation_delay_blocks∈[100,10000]、max_clock_drift_ms∈[0,60000]、forfeit_deposit_ratio∈[10,200]、challenge_deposit_ratio∈[1,100]、challenge_reward_ratio∈[10,100]、archive_retention_blocks∈[1000,1000000]）（**SEC-C2 修复 — `validator_set_size ∈ [5, 1000]`**：强制主网 validator 集下限 5，与 OffChain 模式 |V|>=5 约束对齐，列入 90% quorum 敏感参数）（**SEC-H1 修复 — `max_partial_checkin_count ∈ [1, 10]`**：partial_checkin 提交次数上限，默认 3，防操作方无限拖延）
  - [ ] SubTask 33.4: **NEW-M12 修复 — 可治理参数完整列表（R3-H2 已对齐 spec.md）**：turn_timeout_blocks / hand_max_duration_blocks / dispute_window_blocks / da_window_blocks / recovery_window_blocks / checkpoint_interval_blocks / game_validator_timeout_blocks / ack_deadline_blocks / max_skip_segments / malicious_refuse_threshold / max_interval_ms / max_active_games_per_player / epoch_length_blocks / max_vertex_size / block_gas_limit / tx_prune_after_blocks / vertex_prune_after_blocks / archive_node_min_count / checkpoint_multi_replica_count / delegated_escape_max_expiry_blocks / **defense_window_blocks** / **parameter_delay_blocks** / **epoch_transition_window_blocks** / **bonding_period_blocks** / **slash_percentage** / **downtime_slash_percentage** / **verifier_status** / **downtime_threshold_blocks** / **voting_period_blocks** / **max_designated_operator_check_exemptions** / **under_investigation_threshold**（R4-M3 修正 — 原 4 个参数缺失可治理列表，无法通过治理调整）（R5-H8 修正 — 补全遗漏可治理参数：`max_request_ack_per_turn_timeout`、`max_clock_drift_ms`、`forfeit_deposit_ratio`、`challenge_deposit_ratio`、`challenge_reward_ratio`、`designated_operator_bond_amount`、`unbonding_period_blocks`、`key_rotation_delay_blocks`、`archive_retention_blocks`（R7-M2/M5 修正 — 补全 key\_rotation\_delay\_blocks 与 archive\_retention\_blocks））
  - [ ] SubTask 33.5: Validator 集更新提案（加入/踢出），epoch 边界生效
  - [ ] SubTask 33.6: **NEW-C1 修复 — verifier_status 治理**：`verifier_status` flag（`Stub` / `Production`）由治理设置，初始为 `Stub`；合约层在 `execution_mode = OffChain` 时校验 `verifier_status`，`Stub` 状态下主网 `chain_id` 拒绝 OffChain checkout（返回 `OffChainDisabledOnMainnet`）；治理将 `verifier_status` 升级为 `Production` 后方可主网启用 OffChain 模式；`verifier_status` 为敏感参数，需 90% validator 赞成；**SEC-M4 修复 — verifier_status 命名空间隔离**：`verifier_status` 为 per-`chain_id` 状态（每个网络独立），存储为 `BTreeMap<chain_id, VerifierStatus>` 而非全局单一 flag；防"在 testnet 将 verifier_status 升级为 Production 后通过 fork 复用到 mainnet"或"mainnet 治理误降级 testnet 状态"等命名空间混淆；治理提案须明确目标 `chain_id`，validator 校验提案 `chain_id == network_chain_id` 一致方可生效；testnet/devnet 的 `verifier_status` 由各自网络治理独立设置（testnet 治理 quorum 阈值可低于 mainnet 的 90%）；mainnet 链上硬编码 mainnet `chain_id` 的初始 `verifier_status = Stub`，首次升级为 Production 须经 mainnet 治理 90% quorum + `parameter_delay_blocks` timelock 双重保护
- [ ] Task 34: 跨链桥模块接口（预留，含 M7 安全约束）
  - [ ] SubTask 34.1: 定义 `BridgeHook` trait + `bridge_verify` syscall 接口
  - [ ] SubTask 34.2: `bridge_verify` 必须由协议层在 deposit 流程中调用，不允许任意合约直接调用
  - [ ] SubTask 34.3: 签名绑定 `(nonce, source_chain_id, dest_chain_id, asset, amount, recipient, source_tx_hash)` 防重放（**SEC-H3 修复 — 补全 `recipient` 与 `source_tx_hash` 字段**：`recipient` 绑定 poker_l1 上的接收地址（tagged pubkey 派生地址），防签名被 frontrun 重放到错误接收者 — 原设计缺失 recipient，攻击者可观察合法桥 deposit 签名后抢先提交 bridge_verify 将资产铸造到攻击者地址；`source_tx_hash` 用于跨链追踪）；防重放由 `nonce` + `dest_chain_id` 保证；在 poker_l1 上铸造对应 wrapped 对象给 `recipient`；**SEC2-M1 修复 — bridge_verify 抢跑防护**：bridge_verify tx 须由 recipient 本人签名提交；去中心化 relayer 模式下 gas 奖励 first-come-first-served；recipient 可指定 preferred_relayer 获额外奖励
  - [ ] SubTask 34.4: 反向操作需 burn wrapped 对象 + burn proof（burn-on-source）
  - [ ] SubTask 34.5: 桥验证器插槽注册机制
- [ ] Task 43: Phase 6 模块测试（网络 / RPC / 节点二进制 / 治理 / 跨链桥）
  - [ ] SubTask 43.1: **libp2p gossipsub 单元测试**：2 节点间广播 DAG vertex / tx；peer discovery；sync protocol（按 range 请求 blocks + DAG vertex + fast sync）；in-memory transport mock
  - [ ] SubTask 43.2: **Compact Block Relay 单元测试**：先广播 compact vertex（header + tx short IDs）；接收节点从本地已收 tx 匹配；short ID 冲突处理
  - [ ] SubTask 43.3: **大小上限单元测试**：block <= 4MB / tx <= 128KB / vertex <= max_vertex_size（默认 256KB）；超限返回 `InputTooLarge`
  - [ ] SubTask 43.4: **无 mempool 单元测试**：validator 收到 tx 直接装入下一个 vertex；不维护 gossiped pending tx pool；100ms 缓冲窗口必装 vertex
  - [ ] SubTask 43.5: **客户端多副本广播单元测试**：Public tx + force_* tx（含 force_checkpoint）广播给多 validator 副本；至少一个副本接受即视为成功；checkpoint_anchor 多副本广播作为审查检测证据（副本 validator 仅见证不装入 vertex，签发审查见证证据）
  - [ ] SubTask 43.6: **RPC server 集成测试**：JSON-RPC `get_block` / `get_object` / `get_tx` / `submit_tx` / `get_account` / `get_dag_vertex`；WebSocket 事件订阅；`secp256k1_aggregate_verify` / `bls_verify`（ZK）/ `zk_verify` RPC
  - [ ] SubTask 43.7: **节点二进制集成测试**：validator 节点（含 DAG vertex 产出 + Bullshark 共识 + game sub-block）；full node（validation only + Layer 1-3 裁剪）；archive node（永不裁剪 + `request_historical_data` RPC）；light node（block header 订阅）；CLI keygen（secp256k1 / ed25519 tagged pubkey）
  - [ ] SubTask 43.8: **治理模块单元测试**：参数调整提案 + 投票期 `voting_period_blocks`；2/3 validator 赞成 → 参数生效；validator 集更新提案（加入 / 踢出）+ epoch 边界生效；可治理参数完整列表（含 ack_deadline_blocks / max_skip_segments / tx_prune_after_blocks / vertex_prune_after_blocks / archive_node_min_count / checkpoint_multi_replica_count / delegated_escape_max_expiry_blocks / recovery_window_blocks）
  - [ ] SubTask 43.9: **跨链桥接口单元测试**：`BridgeHook` trait + `bridge_verify` syscall；桥验证器签名背书 + nonce 防重放；`bridge_verify` 必须由协议层调用（合约直接调用返回 `NotAuthorized`）；burn-on-source 约束
  - [ ] SubTask 43.10: **模糊测试**：RPC 接口 + 桥接口 + gossipsub 至少 10000 个随机输入；非法 RPC 参数 / 伪造桥签名 / 超大 vertex 被正确拒绝
  - [ ] SubTask 43.11: **覆盖率门禁**：Phase 6 模块行覆盖率 >= 80%，治理 / 桥接防重放 / gossipsub 安全路径 >= 95%

## Phase 7: 集成与测试
- [ ] Task 35: 端到端测试
  - [ ] SubTask 35.1: 部署 poker contract，创建 table，join，buyin
  - [ ] SubTask 35.2: 走完一局全链上（shuffle → bet → reveal → settle）
  - [ ] SubTask 35.3: 走完一局 OffChain（checkout → checkpoint_anchor → offline → checkin）
  - [ ] SubTask 35.4: secp256k1 与 ed25519 钱包各发起一笔 tx
  - [ ] SubTask 35.5: 验证游戏操作 tx 全程未扣 gas，台费正确扣除（含底池为 0 场景）
  - [ ] SubTask 35.6: 验证时间共识：height 单调递增，timestamp 单调不减 + max_interval 约束，超时以 height 判定
  - [ ] SubTask 35.7: 验证 Narwhal-Bullshark DAG 共识：多 validator 并行出 vertex + Bullshark 排序 + DAG commit finality
  - [ ] SubTask 35.8: 验证游戏分配：Game 创建时 assigned_validator 正确分配 + epoch 切换时重分配
  - [ ] SubTask 35.9: 验证 validator 失败自动接管：某 validator 离线后 tx 仍上链（DAG 冗余）
  - [ ] SubTask 35.10: 验证 tx 重放保护：chain_id + nonce 拒绝重放
  - [ ] SubTask 35.11: 验证活跃 Game 上限：第 11 个 join 被拒绝
  - [ ] SubTask 35.12: 验证 vertex 内排序：GameTurn tx 优先于 force_sync tx
  - [ ] SubTask 35.13: 验证 BLS 子群检查：非子群输入被拒绝
  - [ ] SubTask 35.14: 验证合约升级：UpgradeCap 持有者能部署新版本
  - [ ] SubTask 35.15: 验证链下通信协议：checkpoint_anchor（CheckpointAnchor 通道多副本见证）+ 多方 ACK + force_advance/force_checkin 流程 + force_checkpoint 逃生
  - [ ] SubTask 35.15a: **验证审查截断防护 — force_checkpoint 逃生（NEW-M3/H1 修复）**：assigned_validator 拒收 checkpoint_anchor → 副本 validator 见证（**≥3 个，3-of-5 多签，NEW-M3**）→ force_checkpoint 提交（附 assigned_validator_failure_proof，走 Public 通道正常计费 gas）→ 链上验证接受 → last_action_height 正确更新 → force_advance 不误触发 → **NEW-H1：assigned_validator 标记 under_investigation + defense_window_blocks 防御窗口**（窗口内申辩免责 / 窗口外治理 slashing）
  - [ ] SubTask 35.15a2: **验证审查截断防护 — 委托逃生**：操作方离线 → watchtower/参与者凭 delegated_escape_authorization 代提交 force_checkpoint → 链上验证委托凭证 + 代提交方签名接受 → last_action_height 更新
  - [ ] SubTask 35.15a3: **验证审查截断防护 — 多副本检测（NEW-M3 修复）**：副本 validator 收到 checkpoint_anchor 但 assigned_validator 未装入 vertex → 副本 validator 签发审查见证证据 → 独立提交治理 slashing 提案 → 验证通过；**NEW-M3**：见证签名不足 3 个（仅 2 个）→ 拒绝（防 2-of-N 合谋）
  - [ ] SubTask 35.15a4: **验证 GameTurn fallback（NEW-H2 修复）**：assigned_validator 在 `game_validator_timeout_blocks` 内未装入 GameTurn tx → 客户端向非 assigned_validator 提交（附 `assigned_validator_timeout_proof`）→ 验证 timeout_proof 后接受装入 vertex → 走 Public 通道正常计费 gas → 使用 `gameturn_nonce`（NEW-M9）→ 更新 `last_action_height`
  - [ ] SubTask 35.15a5: **验证 Epoch OffChain 过渡（NEW-M10 修复）**：OffChain 模式 Game 在 epoch 边界前 `epoch_transition_window_blocks` 内提交过渡锚点 → 新 assigned_validator 接管 → `last_partial_fold` 状态保留 → 后续 `partial_checkin` / `checkin` 校验 `intermediate_commitment` 连续性通过
  - [ ] SubTask 35.15a6: **验证 request_ack 频率限制（NEW-M7 修复）**：同一 `(game_id, target_participant)` 在 `ack_deadline` 未过期前提交新 `request_ack` → 返回 `PendingAckExists`；不同 P 的 `request_ack` 不受限制
  - [ ] SubTask 35.15b: **验证审查截断防护 — ack_deadline opt-out**：参与者恶意拒 ACK → 操作方提交 request_ack → ack_deadline 逾期 → opt_out_ack_proof 验证通过 → checkpoint_anchor 接受（或 force_checkpoint 接受，若 checkpoint_anchor 被审查）
  - [ ] SubTask 35.15c: **验证审查截断防护 — refuse_ack dispute**：参与者提交 refuse_ack + 有效 evidence → 进入 dispute；evidence 无效 → 参与者 forfeit 保证金；累计 3 次 → 治理 slashing
  - [ ] SubTask 35.15d: **验证审查截断容错 — checkpoint_skip**：中间 checkpoint 失败 → 操作方提交 checkpoint_skip → skip_count 累计 → 最终 checkin π 验证 skip_count + segment_continuity_proof 通过；skip_count > max_skip_segments → 强制 request_revert
  - [ ] SubTask 35.15e: **验证操作方故障恢复 — 阶段 1 恢复**：操作方机器故障 → 在 turn_timeout_blocks 内恢复 → 提交 checkpoint_anchor → 游戏继续，无 forfeit
  - [ ] SubTask 35.15f: **验证操作方故障恢复 — 阶段 2 重折叠（含 H4/H5）**：操作方故障 + 参与者有动作日志（含 operator_ack_signature）→ 汇集日志 → 校验双签有效 → 从最后 ACKed checkpoint 重新折叠 → force_checkin 成功 → 游戏正常结算，操作方不 forfeit（H4：last_checkpoint_age > turn_timeout_blocks → 机器故障 → 不 forfeit）；**H5 验证**：操作方对 A/B 签同一 step_index 不同 action → 双签检测 → 操作方 forfeit；单方签名 → 以带 ack 条目为准；伪造 operator_ack_signature → 伪造方 forfeit
  - [ ] SubTask 35.15g: **验证操作方故障恢复 — 阶段 3 forfeit**：da_window_blocks + recovery_window_blocks 过期 + 无 force_checkin + 操作方未恢复 → force_revert → 操作方 forfeit 保证金 → 游戏从最后 ACKed checkpoint 继续
  - [ ] SubTask 35.15h: **验证操作方故障恢复 — 无动作日志**：参与者未保存动作日志 → 无法重折叠 → 只能等阶段 3 force_revert
  - [ ] SubTask 35.16: 验证 challenge_delta 语义：从 π public_io 重派生 Δ' 对比 Δ
  - [ ] SubTask 35.17: 验证状态裁剪：结算后历史版本可裁剪，state root 仍可验证
  - [ ] SubTask 35.17a: **验证历史 tx 压缩**：block 距 finality 过 `tx_prune_after_blocks` + 所有 Game 结算 → tx 内容裁剪为 (tx_hash, tx_type, merkle_proof)；tx_merkle_root 仍可验证 tx 存在性
  - [ ] SubTask 35.17b: **验证 DAG vertex 压缩**：vertex 过 `vertex_prune_after_blocks` → tx_list + parent_hashes 裁剪，保留 vertex commitment；Bullshark 共识仍正常运行
  - [ ] SubTask 35.17c: **验证 ZK proof 归档**：Game 结算 + dispute 过期 → π 移到 Walrus，链上仅留 (proof_hash, verification_result, walrus_blob_id)；通过 walrus_blob_id 可恢复完整 proof
  - [ ] SubTask 35.17d: **验证节点角色分层**：archive node 保留全数据 / full node Layer 1-3 裁剪 / light node 仅 block header；full node 通过 `request_historical_data` RPC 从 archive node 获取已裁剪数据；archive node < 3 个时 full node 自动升级
  - [ ] SubTask 35.18: 验证非游戏交易（转账 / 合约部署 / checkin 结算 / bridge 操作）正常计费
  - [ ] SubTask 35.19: 验证资产锁定与铸造场景：买入锁仓、结算分配、台费收款、forfeit 补偿均正确
  - [ ] SubTask 35.20: 验证治理参数调整 + validator 集更新
  - [ ] SubTask 35.21: 验证网络层约束：block <= 4MB、tx <= 128KB、vertex <= 256KB、Compact Block Relay、无 mempool
  - [ ] SubTask 35.22: 验证 slashing：vertex equivocation 双签证据 → 踢出 + 罚没（**NEW-M15**：`slash_amount = stake * slash_percentage / 100`，默认 100%）；停机 slashing（**NEW-L2 + R4-L1/R5-H1**：10% 罚没 + 失去出块资格）
  - [ ] SubTask 35.23: 验证客户端本地路由发现：`hash(game_id, epoch) % |V|` 计算结果与链上一致
  - [ ] SubTask 35.24: **验证 NEW-L1 修复 — secp256k1 low-s**：high-s 签名（`s > n/2`）被拒绝返回 `InvalidSignatureLowS`；low-s 签名正常接受；防 malleability（无法从已上链 tx 签名派生另一有效签名）
  - [ ] SubTask 35.25: **验证 NEW-L4 修复 — ObjectID 唯一性**：`ObjectID = (creator_address, creation_nonce)` 全局唯一；同一 creator nonce 单调递增不复用；不同 creator address 不碰撞；冲突返回 `ObjectIDCollision`
  - [ ] SubTask 35.26: **验证 NEW-C1 修复 — OffChain mainnet gate**：`verifier_status = Stub` 时主网 `chain_id` OffChain checkout 被拒绝（返回 `OffChainDisabledOnMainnet`）；治理升级为 `Production` 后 OffChain checkout 通过；testnet/devnet 不受限制
  - [ ] SubTask 35.27: **验证 NEW-M8 修复 — 治理 timelock + bounds**：参数提案通过后进入 `parameter_delay_blocks` timelock 延迟期；超出上下界的提案被拒绝（如 `turn_timeout_blocks = 2` < 3 被拒）；敏感参数需 90% 阈值
  - [ ] **R5-H4 e2e**: sparse Merkle tree 非包含证明 — tx_hash 不在 vertex 中时非包含证明验证通过，tx_hash 在 vertex 中时验证失败
  - [ ] **R5-H5 e2e**: ValidatorSetUpdate hash chain — 中间 tx 被隐瞒时轻客户端检测到 prev_validator_set_hash 不匹配
  - [ ] **R5-H6 e2e**: continuity_proof 终态验证 — 连续 skip 段间 end_state != start_state 时 checkin 拒绝
  - [ ] **R5-H7 e2e**: validator unbonding 期 — equivocation 后立即退出，unbonding 期内仍可 slashing
  - [ ] **R5-M2 e2e**: 小 validator 集 N=3 — checkpoint_multi_replica_count 自动降为 2，阈值 2-of-2
  - [ ] **R5-M4 e2e**: check_exemptions 重置 — designated operator 提交 checkpoint_anchor 后计数器重置为 0
  - [ ] **R4-H5 e2e（R7-LOW-2 补全）**: under_investigation_count 衰减机制 — validator 被指控 force_checkpoint 后 under_investigation_count +1，1 个 epoch 内无新指控后 count 减 1（最低为 0），防历史指控永久累积
  - [ ] **R7-M6 e2e**: 阶段 3 操作方抢跑 technical_interrupt 被拒 — 操作方在阶段 3（da_window_blocks + recovery_window_blocks 过期后）提交 force_revert(reason=technical_interrupt) → 返回 `OperatorCannotClaimTechnicalInterrupt`；非操作方参与者提交 force_revert(reason=malicious_withholding) → 操作方 forfeit
  - [ ] **R7-H2 e2e**: unbonding_period_blocks 边界校验 — 治理提案设为 0 或 < epoch_length_blocks → 投票期即拒绝；设为 epoch_length_blocks → 通过
- [ ] Task 36: 性能基准
  - [ ] SubTask 36.1: 多 validator DAG 并行 TPS 基准
  - [ ] SubTask 36.2: BLS12-381 syscall 单次延迟（含子群检查）
  - [ ] SubTask 36.3: Hypernova fold step 延迟
  - [ ] SubTask 36.4: Groth16 / IPA verifier 延迟
  - [ ] SubTask 36.5: DAG vertex 传播延迟 + Bullshark 共识延迟
- [ ] Task 37: 文档
  - [ ] SubTask 37.1: 节点部署文档（含 genesis 配置：validator 集、初始参数、epoch 配置）
  - [ ] SubTask 37.2: 合约开发文档（Rust → BPF + gas 计费表 + UpgradeCap）
  - [ ] SubTask 37.3: 链下证明开发文档（Hypernova / Groth16 / IPA + checkpoint 协议）
  - [ ] SubTask 37.4: 跨链桥扩展文档（含安全约束）
  - [ ] SubTask 37.5: 治理操作文档（参数调整 + validator 集更新）
  - [ ] SubTask 37.6: DAG 共识运维文档（vertex 同步、故障排查、validator 加入/退出流程）

# Task Dependencies
- Task 2、3、4、5、6 依赖 Task 1
- Task 7、8、9、10 依赖 Task 2、3、5、6
- Task 11 依赖 Task 2、3
- Task 12 依赖 Task 2、13（validator 集）
- Task 13 依赖 Task 3、5（secp256k1 签名）、9（DAG commit）
- Task 14、15、16、17 依赖 Task 2、5、6
- Task 18、19、20 可与 Task 14 并行（仅依赖 Task 1 与 poker_protocol）
- Task 21 依赖 Task 2、18
- Task 22 依赖 Task 14（syscall 注册）
- Task 23、24、25 依赖 Task 22（23、24 复用 Task 18 pairing）
- Task 26 依赖 Task 23
- Task 27 依赖 Task 11、21、5（tagged pubkey ack 签名）、12（assigned_validator 路由）
- Task 28 依赖 Task 11、21、23、27
- Task 29 依赖 Task 2、4
- Task 30、31、32 依赖 Task 4、9、13、14
- Task 33 依赖 Task 13（validator 集）
- Task 34 可与 Task 30 并行（仅依赖 Task 14 syscall 接口）
- Task 35 依赖 Task 28、29、32、33、34、38、39、40、41、42、43（端到端测试需所有模块测试通过）
- Task 36 依赖 Task 35
- Task 37 可与 Task 36 并行
- Task 38 依赖 Task 1、2、3、4、5、6（Phase 1 全部完成后跑模块测试）
- Task 39 依赖 Task 7、8、9、10、11、12、13（Phase 2 全部完成后跑模块测试）
- Task 40 依赖 Task 14、15、16、17（Phase 3 全部完成后跑模块测试）
- Task 41 依赖 Task 18、19、20（Phase 4 全部完成后跑模块测试）
- Task 42 依赖 Task 21、22、23、24、25、26、27、28、29（Phase 5 全部完成后跑模块测试）
- Task 43 依赖 Task 30、31、32、33、34（Phase 6 全部完成后跑模块测试）
