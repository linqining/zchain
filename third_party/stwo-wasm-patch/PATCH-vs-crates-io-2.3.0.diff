diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/Cargo.toml stwo-2.3.0/Cargo.toml
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/Cargo.toml	2006-07-24 09:21:28
+++ stwo-2.3.0/Cargo.toml	2026-09-13 04:25:40
@@ -1,3 +1,17 @@
+# VENDORED PATCH — provenance (zchain stwo-wasm path A):
+# - Source: crates.io `stwo` 2.3.0, vendored at third_party/stwo-wasm-patch/stwo-2.3.0.
+# - Local changes vs the registry copy (functional): added feature
+#   `wasm-poseidon` (default-on) and gated the poseidon252 family on it
+#   instead of `#[cfg(not(target_arch = "wasm32"))]` (15 sites, 8 files).
+#   starknet-crypto 0.6.2 compiles for wasm32-unknown-unknown (zchain
+#   stwo-wasm-probe evidence), so the original target gate is preventive.
+# - Local changes vs the registry copy (metadata only):
+#   * `[workspace]` empty table added so this vendored crate is its own
+#     workspace root (isolation from the zchain root workspace; same
+#     pattern as stwo-wasm-probe).
+# - Everything else identical. See docs/stwo-wasm-path-a.md for the
+#   `diff -r` attestation and the upstream issue draft.
+#
 # THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
 #
 # When uploading crates to the registry Cargo will automatically
@@ -27,8 +41,11 @@
 [package.metadata.cargo-machete]
 ignored = ["stwo-std-shims"]
 
+[workspace]
+
 [features]
-default = ["std"]
+default = ["std", "wasm-poseidon"]
+wasm-poseidon = []
 parallel = [
     "rayon",
     "indexmap/rayon",
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/channel/mod.rs stwo-2.3.0/src/core/channel/mod.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/channel/mod.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/core/channel/mod.rs	2026-09-13 04:25:40
@@ -1,3 +1,23 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 use core::fmt::Debug;
 
 use std_shims::Vec;
@@ -5,9 +25,9 @@
 use super::fields::qm31::SecureField;
 use crate::core::vcs_lifted::merkle_hasher::MerkleHasherLifted;
 
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 mod poseidon252;
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 pub use poseidon252::Poseidon252Channel;
 
 mod blake2s;
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/vcs/mod.rs stwo-2.3.0/src/core/vcs/mod.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/vcs/mod.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/core/vcs/mod.rs	2026-09-13 04:25:40
@@ -1,3 +1,23 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 //! Vector commitment scheme (VCS) module.
 
 pub mod blake2_hash;
@@ -6,7 +26,7 @@
 pub mod hash;
 mod merkle_hasher;
 pub use merkle_hasher::MerkleHasher;
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 pub mod poseidon252_merkle;
 #[cfg(all(test, feature = "prover"))]
 pub mod test_utils;
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/vcs_lifted/mod.rs stwo-2.3.0/src/core/vcs_lifted/mod.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/vcs_lifted/mod.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/core/vcs_lifted/mod.rs	2026-09-13 04:25:40
@@ -1,6 +1,26 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 pub mod blake2_merkle;
 pub mod merkle_hasher;
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 pub mod poseidon252_merkle;
 #[cfg(feature = "prover")]
 pub mod test_utils;
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/vcs_lifted/verifier.rs stwo-2.3.0/src/core/vcs_lifted/verifier.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/core/vcs_lifted/verifier.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/core/vcs_lifted/verifier.rs	2026-09-13 04:25:40
@@ -1,3 +1,23 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 use hashbrown::HashMap;
 use itertools::Itertools;
 use serde::{Deserialize, Serialize};
@@ -225,7 +245,7 @@
     }
 
     #[test]
-    #[cfg(not(target_arch = "wasm32"))]
+    #[cfg(feature = "wasm-poseidon")]
     fn test_merkle_success_poseidon() {
         use crate::core::vcs_lifted::poseidon252_merkle::Poseidon252MerkleHasher;
         let (queries, decommitment, values, verifier) = prepare_merkle::<Poseidon252MerkleHasher>();
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/cpu/mod.rs stwo-2.3.0/src/prover/backend/cpu/mod.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/cpu/mod.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/prover/backend/cpu/mod.rs	2026-09-13 04:25:40
@@ -1,3 +1,23 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 pub mod accumulation;
 mod blake2s;
 pub mod circle;
@@ -5,7 +25,7 @@
 mod grind;
 pub mod lookups;
 mod merkle_lifted;
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 mod poseidon252;
 pub mod quotients;
 
@@ -17,7 +37,7 @@
 use super::{Backend, BackendForChannel, Column, ColumnOps};
 use crate::core::utils::bit_reverse;
 use crate::core::vcs_lifted::blake2_merkle::{Blake2sM31MerkleChannel, Blake2sMerkleChannel};
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 use crate::core::vcs_lifted::poseidon252_merkle::Poseidon252MerkleChannel;
 use crate::prover::lookups::mle::Mle;
 use crate::prover::poly::circle::{CircleCoefficients, CircleEvaluation};
@@ -28,7 +48,7 @@
 impl Backend for CpuBackend {}
 impl BackendForChannel<Blake2sMerkleChannel> for CpuBackend {}
 impl BackendForChannel<Blake2sM31MerkleChannel> for CpuBackend {}
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 impl BackendForChannel<Poseidon252MerkleChannel> for CpuBackend {}
 
 impl<T: Debug + Clone + Default + Send + Sync> ColumnOps<T> for CpuBackend {
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/simd/grind.rs stwo-2.3.0/src/prover/backend/simd/grind.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/simd/grind.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/prover/backend/simd/grind.rs	2026-09-13 04:25:40
@@ -1,3 +1,23 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 use std::simd::cmp::SimdPartialOrd;
 use std::simd::num::SimdUint;
 use std::simd::u32x16;
@@ -139,7 +159,7 @@
     found.expect("Grind failed to find a solution.")
 }
 
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 pub mod poseidon252 {
     use starknet_crypto::poseidon_hash_many;
     use starknet_ff::FieldElement as FieldElement252;
@@ -210,7 +230,7 @@
         }
     }
 
-    #[cfg(not(target_arch = "wasm32"))]
+    #[cfg(feature = "wasm-poseidon")]
     #[test]
     fn test_grind_poseidon() {
         let pow_bits = 10;
@@ -242,7 +262,7 @@
         test_grind_is_deterministic::<Blake2sChannel>();
     }
 
-    #[cfg(not(target_arch = "wasm32"))]
+    #[cfg(feature = "wasm-poseidon")]
     #[test]
     fn test_grind_poseidon_is_deterministic() {
         test_grind_is_deterministic::<crate::core::channel::Poseidon252Channel>();
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/simd/mod.rs stwo-2.3.0/src/prover/backend/simd/mod.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/simd/mod.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/prover/backend/simd/mod.rs	2026-09-13 04:25:40
@@ -1,8 +1,28 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 use serde::{Deserialize, Serialize};
 
 use super::{Backend, BackendForChannel};
 use crate::core::vcs_lifted::blake2_merkle::{Blake2sM31MerkleChannel, Blake2sMerkleChannel};
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 use crate::core::vcs_lifted::poseidon252_merkle::Poseidon252MerkleChannel;
 
 pub mod accumulation;
@@ -21,9 +41,9 @@
 mod grind;
 pub mod lookups;
 pub mod m31;
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 pub mod poseidon252;
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 pub mod poseidon252_lifted;
 pub mod prefix_sum;
 pub mod qm31;
@@ -37,7 +57,7 @@
 impl Backend for SimdBackend {}
 impl BackendForChannel<Blake2sMerkleChannel> for SimdBackend {}
 impl BackendForChannel<Blake2sM31MerkleChannel> for SimdBackend {}
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 impl BackendForChannel<Poseidon252MerkleChannel> for SimdBackend {}
 
 // Optimal chunk sizes were determined empirically on an intel 155u machine.
diff -ru /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/simd/poseidon252.rs stwo-2.3.0/src/prover/backend/simd/poseidon252.rs
--- /Users/mac/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/stwo-2.3.0/src/prover/backend/simd/poseidon252.rs	2006-07-24 09:21:28
+++ stwo-2.3.0/src/prover/backend/simd/poseidon252.rs	2026-09-13 04:25:40
@@ -1,3 +1,23 @@
+// VENDORED PATCH — provenance (zchain stwo-wasm path A):
+// - Source: crates.io `stwo` 2.3.0 (sha of origin dir: registry
+//   index.crates.io-1949cf8c6b5b557f/stwo-2.3.0), vendored at
+//   third_party/stwo-wasm-patch/stwo-2.3.0 (zchain repo).
+// - Local change (the ONLY functional change across the vendored tree):
+//   the poseidon252 family exclusion gate
+//   `#[cfg(not(target_arch = "wasm32"))]` was converted to the explicit
+//   feature gate `#[cfg(feature = "wasm-poseidon")]` (default-on; see
+//   Cargo.toml [features]) at every site that excluded Poseidon252Channel /
+//   Poseidon252MerkleHasher / the simd+cpu backend impls on wasm32.
+//   Rationale: starknet-crypto 0.6.2 (this crate's poseidon dependency) is
+//   pure Rust and compiles for wasm32-unknown-unknown (measured: zchain
+//   stwo-wasm-probe, logs/wasm_check_*.txt EXIT=0), so the original gate is
+//   preventive, not a technical constraint. Feature gate instead of removing
+//   the gate entirely keeps upstream's ability to build a
+//   poseidon-free wasm verifier, while default-on makes wasm behave like
+//   native for existing consumers.
+// - Upstream issue draft: docs/stwo-wasm-path-a.md (zchain repo) §"Upstream issue draft".
+// - All other lines are byte-identical to the registry copy (`diff -r` attested
+//   in docs/stwo-wasm-path-a.md).
 use itertools::Itertools;
 #[cfg(feature = "parallel")]
 use rayon::prelude::*;
@@ -6,7 +26,7 @@
 use super::SimdBackend;
 use crate::core::fields::m31::BaseField;
 use crate::core::vcs::poseidon252_merkle::Poseidon252MerkleHasher;
-#[cfg(not(target_arch = "wasm32"))]
+#[cfg(feature = "wasm-poseidon")]
 use crate::core::vcs::MerkleHasher;
 use crate::parallel_iter;
 use crate::prover::backend::{Col, Column, ColumnOps};
