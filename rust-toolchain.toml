# Stwo 2.3.0 prover feature 需要 nightly Rust（#![feature(iter_array_chunks, portable_simd, slice_ptr_get)]）。
# 固定 nightly-2026-04-15 保证 CI/本地构建一致。详见 .trae/documents/stwo_migration_phase1_2_1_3_plan.md
[toolchain]
channel = "nightly-2026-04-15"
components = ["rustfmt", "clippy"]
profile = "default"