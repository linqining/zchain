# 审核 + 移除过期 Hypernova + 架构审核 + 文档更新 实施计划

## 现状(审核已完成)
- **Hypernova 已废弃**,全部残留为:① 文档(5 篇)② 代码注释 ③ 别名/常量名(`SCHEME_HYPERNOVA`=deprecated alias、`GAS_HYPERNOVA_VERIFY`、`GasHypernovaVerify` 治理参数、`register_hypernova_stub_verifier`)。
- **真实 ZK 实现** = `poker_texas_air`(21 个自定义 AIR 电路 + Stwo,经 `proving_service` 离线证明) + `poker_zkvm`(通用 Stwo Circle-STARK zkVM)。链上 `zk_verify` 当前 dormant。
- **5-crate workspace**,非文档所述"2 crate + 1 bin"。
- `poker_l1/src/lib.rs:13-14` 注释称 5 个模块"为 stub"——**已过期**(均已成实现)。
- `docs/00-architecture-overview.md` 第 4 节整段描述已删除的 hypernova/ccs/fold 模块,最过期。

## 用户决策
- 代码标识符:**全面重命名**(hypernova→stwo/texas_air),保留 governance RPC 字符串 `gas_hypernova_verify` 作向后兼容别名。
- 过期文档:**原地重写**(保留文件名/编号)。

---

## 第 1 步:全面重命名 hypernova 代码标识符

### 1a. vm-common(共享层)
- `vm-common/src/gas.rs`:`GAS_HYPERNOVA_VERIFY` → `GAS_STWO_VERIFY`(常量名);`zk_verify_gas()` 内 `1 => GAS_HYPERNOVA_VERIFY` → `GAS_STWO_VERIFY`;注释更新。
- `vm-common/src/syscall_id.rs`:`ZkVerify` 注释 "Hypernova/Groth16/IPA" → "Stwo/Groth16/IPA"。
- `vm-common/src/catalog.rs`:precompile 描述 "ZK 证明验证(Hypernova/...)" → "Stwo/..."。
- `vm-common/CONTRACT_DEV_GUIDE.md`(若有 hypernova):同步更新。

### 1b. poker_l1
- `offline/zk_verifier.rs`:删除 `SCHEME_HYPERNOVA` deprecated 别名(line 25-26)与 `register_hypernova_stub_verifier`(line 441-445)——这两个是死代码(deprecated,仅测试用);更新 `register_hypernova_stub_verifier` 的测试调用点改为 `register_stwo_verifier`。
  - 受影响测试:`tests/soundness_tests.rs:587`、`offline/state.rs`(多处测试用 SCHEME_HYPERNOVA)。
- `governance/mod.rs`:`GasHypernovaVerify` → `GasStwoVerify`(变体);`gas_hypernova_verify` 字段 → `gas_stwo_verify`;**`as_str()` 保留返回 `"gas_hypernova_verify"` 作向后兼容别名**(加注释说明);`DEFAULT_GAS_HYPERNOVA_VERIFY` → `DEFAULT_GAS_STWO_VERIFY`。
- `vm/gas_table.rs`:re-export `GAS_HYPERNOVA_VERIFY` → `GAS_STWO_VERIFY`;测试更新。
- `vm/syscalls.rs`、`rpc/mod.rs`、`offline/state.rs`、`offline/mod.rs`:注释更新(hypernova→stwo)。

### 1c. poker_zkvm
- `src/lib.rs`:模块文档更新(删除 hypernova 迁移历史赘述,改为简洁的 Stwo Circle-STARK 说明)。
- `test_helpers.rs`、`stwo_backend/trace_native.rs`、`stwo_backend/mod.rs`:注释更新。

### 1d. 验证
- `cargo build --workspace` + `cargo test -p poker_l1 --lib` + `cargo test -p vm-common` 全绿。
- grep 确认无遗留 `hypernova` 标识符(仅 governance `as_str()` 兼容字符串 + 文档历史说明保留)。

---

## 第 2 步:修正过期代码注释

- `poker_l1/src/lib.rs:13-14`:删除"当前为 stub"断言,改为反映各模块已实现(network/bridge/governance/node/offline 均为实质实现;crypto_precompiles 部分实现)。

---

## 第 3 步:原地重写 5 篇过期文档

### 3a. `docs/00-architecture-overview.md`(最高优先)
- §2 workspace:从"2 crate + 1 bin"改为 **5 crate**(poker_l1/poker_texas_air/vm-common/proving_service/poker_zkvm),更新 Mermaid 图。
- §4 整段重写:删除 hypernova/ccs/fold/cyclegold 描述,改为 **poker_zkvm(Stwo Circle-STARK)+ poker_texas_air(21 自定义 AIR)+ proving_service** 三层 ZK 架构;说明链上 zk_verify 当前 dormant、真实证明走 proving_service 离线 host-verify。
- §3.2 模块清单:补充 executor/bridge/governance/sync/indexer 等已实现模块。
- §5/§9/§10:hypernova→stwo/poker_texas_air。
- §13.1 缺失模块表:更新为当前真实状态(network 仍无 libp2p transport;其余基本完整)。
- §13.4 已实现模块摘要:补充本轮(共识签名验证/ECVRF/多 validator BFT/AccountStore 持久化/Bridge 接线/gas→proposer/staking/原生转账)。

### 3b. `docs/37-3-offline-proof-development.md`
- 标题与正文:删除 `offline/{ccs,hypernova,groth16,ipa}.rs` 引用(文件不存在);改为 `offline/{state,zk_verifier,ack_chain}.rs` + Stwo 路径;说明离线证明走 `proving_service` → `poker_texas_air::Orchestrator`。

### 3c. `docs/37-2-contract-development.md`
- line 272:gas 表 `GAS_HYPERNOVA_VERIFY = 50000`(错误值)→ `GAS_STWO_VERIFY = 300000`。

### 3d. `docs/37-7-rpc-interface.md`
- zk_verify RPC:scheme_id 1 的描述 "Hypernova" → "Stwo"。

### 3e. `docs/37-10-trust-layer-model.md`
- 信任基础:hypernova → Stwo + host-verified receipts(poker_texas_air Layer 1)。

---

## 第 4 步:架构审核交付物

在 `00-architecture-overview.md` 新增/更新"架构审核(2026-08)"小节,记录:
- 模块完整性矩阵(10 子系统评级,基于审核)。
- 已知缺口:network 无 libp2p transport、链上 zk_verify dormant(证明走离线)、poker_texas_air 递归闭环未完成(21 AIR 为 PoC)。
- 本轮硬化成果汇总(共识签名验证/ECVRF/多 validator/AccountStore/Bridge/代币-staking)。

---

## 验证标准(完成审计)
1. `cargo build --workspace` 通过。
2. `cargo test -p poker_l1 --lib` + `cargo test -p vm-common --lib` 全绿(标识符重命名不破坏测试)。
3. `grep -rni "hypernova" poker_l1/src vm-common/src poker_zkvm/src` 仅剩:governance `as_str()` 兼容字符串 + 明确的"历史命名"注释。
4. `docs/00-architecture-overview.md` workspace/ZK/模块表反映 5-crate + Stwo + poker_texas_air 现状。
5. `poker_l1/src/lib.rs` 无"为 stub"过期断言。

## 不做的事
- 不删除 `.trae/documents/` 历史规划文档(非用户面向,量大)。
- 不重写内容仍准确的 37-1/37-4/37-5/37-6/37-8/37-9 文档。
- 不动 governance RPC 字符串 `gas_hypernova_verify`(向后兼容)。
- 不实现新的 zk_verify 集成(架构审核记录缺口,但不修)。
- 不动 poker_lean/(非 workspace 成员)。