{
  "name": "zchain-extension",
  "version": "0.1.0",
  "private": true,
  "description": "ZChain browser wallet extension (plan-appchain §6.12.4 Extension 0.1). Not published to npm; exists so node --test runs the ES-module test suite and the service worker's ES-module imports resolve consistently.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js tests/adapters/*.test.js"
  }
}
